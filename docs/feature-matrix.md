# NOVA PIP Feature Matrix

A living engineering inventory — not user documentation. First drafted as part of a Stabilization & Acceptance Sprint, from git history and test coverage. Updated after the user's full acceptance walkthrough of the deployed application. Feature work tracked here belongs to the Knowledge Intelligence initiative.

**Status legend**

| Symbol | Meaning |
|---|---|
| ⚙️ Implemented | Code exists (see the Branch column — usually a feature branch, not yet `main`), not yet verified in the deployed app |
| ✅ Accepted | Visible in the running application **and verified by the user** |
| 🔲 Backlog | Not implemented, or intentionally deferred |

**Sprint 9.5/10 addendum (Backlog, Technical Debt & Release Scope Review)** — the symbols above answer "does the code exist / has a human seen it work," but not "is this row still open work, or a conscious decision." As of Sprint 9.5/10, every 🔲 row in this file falls into exactly one of the following, resolved in `docs/arriyia-personal-release-backlog.md` (the authoritative classification — consult it, not this legend, for a row's actual disposition):

| Disposition | Meaning |
|---|---|
| Accepted limitation | A conscious trade-off for v1, documented with reasoning, not scheduled |
| Deferred (P2) | Real, unresolved, explicitly not urgent — see the backlog's P2 table |
| Strategic — Business/Enterprise | Belongs to a future ARRIYIA tier, not Personal v1 — see `docs/arriyia-product-roadmap.md` |
| Future / Exploratory | A real idea with no current requirement forcing it |
| Obsolete | No longer has strategic value; safe to ignore or remove |

No 🔲 row in this document should be read as "forgotten" — every one has a home in the backlog above.

Corrected by the Reliability & Truth Audit: this legend previously said ⚙️ meant "merged to `main`," which was never actually true of any current ⚙️ row — the Branch column has always named the feature branch. See `docs/reliability-truth-audit.md` for the full audit; its central finding is that `main` is 11 commits behind this branch and none of the last five feature efforts have reached production, which is why every ⚙️ row below is also, functionally, ❌ Blocked on a deploy rather than on further code work.

**Acceptance pass completed.** The user walked through every ⚙️ Implemented row below against the deployed application and confirmed it working — all such rows are now ✅ Accepted. 🔲 Backlog rows are unaffected (nothing to accept yet). If anything regresses later, flip its row back to ⚙️ and note the issue rather than silently re-marking it ✅.

`Manual` reflects whether the NOVA PIP Manual has a chapter covering this feature. `Tests` reflects whether the module has automated test coverage at all (file existence, not full-coverage confirmation).

---

## Library & Reading

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Document upload (PDF/EPUB/DOCX/TXT/MD) | ✅ | main | ✅ | ✅ | ✅ |
| Collections + tags | ✅ | main | ✅ | ✅ | ✅ |
| Document Detail page | ✅ | main | ✅ | ✅ | ❌ |
| PDF Reader (page rendering + text layer) | ✅ | main | ✅ | ✅ | ❌ |
| EPUB Reader (chapters) | ✅ | main | ✅ | ✅ | ✅ |
| Spreadsheet Reader (xlsx/csv/ods) | ✅ | main | ✅ | ✅ | ✅ |
| Spreadsheet Intelligence (column/type/pattern detection, Analyst Layer, Summary Card) | ✅ | main | ✅ | ✅ | ✅ |
| Image Reader | ✅ | main | ✅ | ✅ | ❌ |
| Image upload + derivatives pipeline (thumbnail/optimized) | ✅ | main | ✅ | ✅ | ✅ |
| Image Lightbox | ✅ | main | ✅ | ✅ | ❌ |
| Mobile image upload fix ("File is empty") | ✅ | main | ✅ | ✅ | ✅ |

## Notes

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Notes CRUD (create/edit/delete) | ✅ | main | ✅ | ✅ | ✅ |
| Note tags | ✅ | main | ✅ | ✅ | ❌ |
| Save conversation → Note | ✅ | main | ✅ | ✅ | ❌ |
| Create note from Reader highlight | ✅ | main | ✅ | ✅ | ❌ |
| Note ↔ Asset linking | ✅ | main | ✅ | ✅ | ❌ |

## Chat & AI

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Multi-conversation Chat page | ✅ | main | ✅ | ✅ | ✅ |
| Provider selection + per-conversation switching | ✅ | main | ✅ | ✅ | ✅ |
| Provider availability detection | ✅ | main | ✅ | ✅ | ✅ |
| Provider fallback chain (multi-hop) | ✅ | main | ✅ | ✅ | ✅ |
| RAG retrieval grounding (document chunks) | ✅ | main | ✅ | ✅ | ✅ |
| Reader Chat Panel (in-reader chat) | ✅ | main | ✅ | ✅ | ✅ |
| NOVA Insight Drawer (Chat) | ✅ | main | ✅ | ✅ | ✅ |
| Reader Insight Drawer (minimize/maximize) | ✅ | main | ✅ | ✅ | ✅ |
| AI Health Dashboard (provider observability) | ✅ | main | ✅ | ✅ | ✅ |
| Memory management (explicit/learned/conversation memory) | ✅ | main | ✅ | ✅ | ✅ |
| Command Bar / NOVA command palette | ✅ | main | ✅ | ✅ | ✅ |

## AI Knowledge Graph & Intelligence

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Knowledge extraction (LLM concepts/entities, manual trigger) | ✅ | main | ✅ | ✅ | ✅ |
| Cross-document relationship detection | ✅ | main | ✅ | ✅ | ✅ |
| Canonical node dedup (resolveCanonicalNode, Phase 9A) | ✅ | main | ✅ | ✅ | ✅ |
| Knowledge Explorer (card grid + filters) | ✅ | main | ✅ | ✅ | ❌ |
| Content Connections graph (documents/notes/highlights/tags, `/knowledge/graph` — distinct from the AI Knowledge Graph below, previously undocumented and mislabeled "Knowledge Graph") | ✅ | main | ✅ | ✅ | ❌ |
| Interactive Concept Graph (SVG, focus/expand/pin) | ✅ | main | ✅ | ✅ | ✅ |
| Graph clustering (connected components) | ✅ | main | ✅ | ✅ | ✅ |
| Deterministic concept matcher (Phase 2B) | ✅ | main | ✅ | ✅ | ✅ |
| Knowledge Node → note/conversation evidence linking (Phase 2B) | ✅ | main | ✅ | ✅ | ❌ |
| Concept Card in Universal Search (Phase 2B) | ✅ | main | ✅ | ✅ | ❌ |
| Node drill-down page (Overview/Related/Timeline, Phase 2B) | ✅ | main | ✅ | ✅ | ❌ |
| Knowledge Confidence scoring (source count/diversity/freshness/relationships) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Knowledge Actions: Merge Notes | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Knowledge Actions: Generate Briefing (concept → grounded Note, linked back as evidence) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Knowledge Actions: Export Knowledge Package (concept → Markdown download) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Knowledge Collections (cross-type curated groupings, spans documents/notes/conversations/images/concepts) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |
| Natural Language Knowledge Commands: "Create an executive briefing on X" (typed into NOVA chat) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| AI Workspace Actions v1: Workspace Action Router (shared command-matching layer in AIService.sendMessage) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| AI Workspace Actions v1: Save to Notes (per-message button + "Save this"/"Remember this"/"Capture this"/"Add this to my notes") | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Node lifecycle (merge/rename/archive) | 🔲 | — | — | — | — |
| Note → Task / Conversation → Project actions | 🔲 | — | — | — | — |
| Explorer virtualization/pagination | 🔲 | — | — | — | — |
| Contradiction detection | 🔲 | — | — | — | — |
| Reading-coverage confidence signal | 🔲 | — | — | — | — |

## Universal Search

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Document search provider (embedding similarity) | ✅ | main | ✅ | ✅ | ❌ |
| Conversation search provider (grouped, scored — Phase 2A) | ✅ | main | ✅ | ✅ | ✅ |
| Notes search provider (Phase 1) | ✅ | main | ✅ | ✅ | ❌ |
| Graph Layer / Concept Card branch (Phase 2B) | ✅ | main | ✅ | ✅ | ❌ |
| Cross-provider ranking refinement (uniform recency bonus, all sources) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Hybrid semantic + lexical search | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ✅ |
| Zero-result recovery (empty library vs. no match) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |

## Knowledge Capture

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Quick Capture dialog + command (documents/images/notes/URLs) | ✅ | main | ✅ | ✅ | ❌ |
| Deployment reconciliation (branch → main drift) | ✅ resolved | main | ✅ | ❌ | — |

## Workspace Intelligence

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Workspace Intelligence Hub (homepage) | ✅ | main | ✅ | ✅ | ✅ |
| Executive Dashboard | ✅ | main | ✅ | ✅ | ✅ |
| Workspace Evolution / timeline | ✅ | main | ✅ | ✅ | ✅ |
| Workspace objectives + knowledge gap detection | ✅ | main | ✅ | ✅ | ✅ |
| Workspace management (create/switch/archive) | ✅ | main | ✅ | ✅ | ✅ |

## Settings & Platform

| Feature | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Provider Control Center | ✅ | main | ✅ | ✅ | ❌ |
| Default provider resolution + overrides | ✅ | main | ✅ | ✅ | ✅ |
| Auth (login/signup/password reset) | ✅ | main | ✅ | ✅ | ❌ |
| Mobile nav drawer | ✅ | main | ✅ | ✅ | ❌ |

---

## Platform Coherence Sprint v1

Not a new feature phase — a correctness/coherence pass across the five Knowledge Intelligence initiative phases above, auditing their integration points and fixing inconsistencies found. Retroactively renamed from "Platform Integration Sprint" to align with the "Platform Coherence" naming this and its v2 successor share: both exist for the same reason — making independently implemented capabilities behave as one product, not a feature addition in themselves. All ⚙️ Implemented, none ✅ Accepted yet.

- Cross-feature integration
- Shared behavior reconciliation
- Knowledge Intelligence consistency

| Fix | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Generate Briefing: search indexing + concept-linking moved into the shared `generateBriefing()` function itself (previously only the concept-page hook did this — briefings created via the chat command were silently unsearchable and unlinked) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (existing coverage) |
| `useKnowledgeNodeDetails` (Explorer + Dashboard insights): now resolves note/conversation sources, not just documents (a pre-Phase-2B filter was silently dropping them) — same underlying data as the Concept Card/drill-down page, now displayed consistently everywhere | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |
| Knowledge Confidence now computed and shown on the Explorer's and Dashboard's concept cards, not just the Concept Card in Search and the drill-down page — same `computeKnowledgeConfidence`, fed from the same batched query, no new fetch | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |
| `AddToCollectionButton` now shows an item's existing collection membership (removable, linked) instead of only offering to add — previously there was no way to see from an item's own page which collections it already belonged to | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |
| Merge Notes carries forward Collection membership to the merged note (previously silently dropped, since the originals — and their membership links — get deleted) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | ❌ |
| Docs: "UX-13" references replaced with "Knowledge Intelligence initiative" per user direction — code comments (git-historical, e.g. "UX-13.11 Phase 2B") were deliberately left as-is; only the living docs (this file, the Manual) were updated | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | — | ✅ | — |

### Remaining known inconsistencies (not fixed this sprint — see Integration Report)

- Universal Search has no Collections branch — collections aren't searchable by name, and flat search results don't indicate collection membership. Building this is a new capability, not composition of existing ones, so it's out of scope for a "no expansion" sprint.
- Deleting a document/note/conversation/asset that's a Collection member leaves an orphaned `knowledge_links` row pointing at nothing — consistent with the pre-existing, already-documented "knowledge_links never cascades on deletion" behavior for graph evidence, not a new gap, but worth deciding whether Collections specifically should behave differently.
- Collection-level actions (export a whole collection, generate a briefing spanning a collection) don't exist — only per-item actions do.

## Notes

- Acceptance pass completed by the user against the deployed application; all ⚙️ Implemented rows promoted to ✅ Accepted as a batch confirmation ("all fine"), not itemized per-row feedback. If a specific row is later found not to work, flip it back to ⚙️ and record what broke — don't silently re-mark it ✅.
- The NOVA PIP Manual (`docs/manual/`) now has all 8 planned chapters, covering every ✅ Accepted feature above. The one exception is "Deployment reconciliation," which is an engineering/ops item rather than a user-facing feature, so it isn't a Manual chapter itself — it's mentioned contextually where relevant.
- Screenshots are not yet part of the Manual — chapters document behavior first; visual capture is a follow-up pass.
- 🔲 Backlog rows (node lifecycle, note→task/conversation→project, Explorer virtualization, contradiction detection, reading-coverage) remain the canonical Knowledge Intelligence initiative remainder, per the roadmap sequencing already agreed: Universal Search maturity → Knowledge Confidence → Knowledge Actions → Knowledge Collections → Natural Language Commands.
- Knowledge Actions v1 deliberately shipped three actions that reuse existing infrastructure end-to-end (Notes CRUD, the capability/prompt-template pattern, knowledge_node_sources provenance) and deferred note→task/conversation→project, since those need new Task/Project schema entities — a real design decision, not something to invent unilaterally mid-phase.
- Knowledge Collections v1 added exactly one new table (`knowledge_collections`, identity/metadata only) — membership reuses the existing generic `knowledge_links` polymorphic-edge table (`source_type='knowledge_collection'`) the same way linkNoteToHighlight/linkNoteToConversation/linkNoteToAsset already do, so no new join table or per-type schema was needed to span documents/notes/conversations/images/concepts in one collection.
- Natural Language Knowledge Commands v1 shipped its first (and so far only) recognized command, "Create an executive briefing on X," typed directly into NOVA chat. No new orchestration pipeline was built — `AIService.sendMessage` deterministically recognizes the phrase (no LLM call spent on intent classification) and, when matched, calls Search (`searchKnowledgeConcepts`) to resolve the topic to a concept, then reuses the exact same `generateBriefing` pipeline the concept drill-down page's own button calls (which itself now also folds in Collections membership, not just Confidence/Knowledge Graph/Notes as before). An unrecognized topic gets a graceful "couldn't find that concept" reply instead of a wasted or hallucinated LLM call.
- All 5 phases of the originally agreed Knowledge Intelligence initiative remainder sequence (Universal Search maturity → Knowledge Confidence → Knowledge Actions → Knowledge Collections → Natural Language Commands) are now ⚙️ Implemented. The initiative itself remains paused pending acceptance; the current objective is Platform Coherence Sprint v1 (see below), not a new feature phase.

## AI Workspace Actions v1

A new, separately named workstream — not a Knowledge Intelligence initiative phase and not part of Platform Coherence Sprint v1. Restores and generalizes Chat's "Save to Notes" functionality behind a reusable pathway, per explicit user direction: `Chat UI → Workspace Action Router → Save Knowledge Action → Create Note → Index → Knowledge linking → Confirmation`.

- **Workspace Action Router** (`src/modules/workspace-actions/`): a small `WorkspaceAction<TPayload> { id, match, run }` registry (`registerWorkspaceAction`/`matchWorkspaceAction`/`runWorkspaceAction`), following the same registration-on-import convention as `coreModule`/`knowledge-intelligence/module.ts`/`search/registerBuiltInProviders`/`commands/registerBuiltInCommands`. Generalizes the single hardcoded `if (parseExecutiveBriefingCommand(text))` branch that used to live inside `AIService.sendMessage` into a real router now that a second natural-language command exists — the "not needed yet, now needed" simplification flagged in the prior Product Readiness Audit. The pre-existing "Create an executive briefing on X" command was migrated into this router (`generateBriefingAction.ts`) unchanged in behavior; it is not a new capability.
- **Save Knowledge Action** (`saveMessageToNote()` in `src/modules/notes/api/saveMessageToNote.ts`): the single function behind both entry points below — Create Note → link to conversation → link to the specific message → index → link known concepts. This is the one save pipeline; neither entry point duplicates note-creation logic.
- **Entry point 1 — per-message Save button**: `MessageBubble` gained an optional `onSave`/`saved`/`saving` prop set, wired from `ChatPage` and `ReaderChatPanel` via a new `useSaveMessageToNote` hook. Available on both the user's own messages and NOVA's replies.
- **Entry point 2 — natural language**: "Save this", "Remember this", "Capture this", "Add this to my notes" (`isSaveToNotesCommand`, pure regex, no LLM call) resolve "this" to the most recent **assistant** message in the conversation — a scoping decision (the natural reading of "save this" said right after a reply), not an oversight; saving the user's own last message is a plausible future variant, not this one.
- **Provenance**: notes saved this way carry `generation_metadata: {savedFrom: 'chat-message', conversationId, messageId, messageCreatedAt}` (same JSONB column Summarize already uses for its own provenance shape) plus two `knowledge_links` rows — `target_type='conversation'` (pre-existing) and the new `target_type='message'` (`linkNoteToMessage`, added to the polymorphic `knowledge_links` table with zero schema changes, mirroring `linkNoteToAsset`).
- **Audit finding fixed in passing**: `SaveConversationDialog` (the existing whole-conversation save dialog) never called `indexNote()`/`linkKnownConceptsToSource()` — the exact same "indexing left to the caller, and the caller forgot" bug class Platform Coherence Sprint v1 fixed for Generate Briefing, independently rediscovered here. Fixed by adding the two missing calls; no behavior change to the dialog's UI or scope options.
- Verified in a real browser against a mocked Supabase backend: per-message Save button renders on both roles, creates the note with correct provenance and both links, transitions to "Saved"; the "Save this" chat command produces NOVA's confirmation reply and creates an equivalent note via the same underlying function. tsc/vitest (947 tests)/lint/build all pass.

## Platform Coherence Sprint v2

Like Platform Coherence Sprint v1, a correctness/coherence pass — not a new feature phase — this time following the Product Readiness Audit. Scope was fixed in advance to four items: Explorer navigation, graph terminology, a shared source-resolution helper, and Collection membership error handling. All ⚙️ Implemented, none ✅ Accepted yet.

- Navigation consistency
- Terminology clarification
- Shared source resolution
- Error-state consistency

| Fix | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Knowledge Explorer cards now open the concept drill-down page (`/knowledge/nodes/:id`) — `KnowledgeCard` gained an optional `to` prop that makes its title a `Link`, reusing the same target `ConceptCard` already links to; previously Explorer cards were static, the only card grid in the app that didn't reach the drill-down page | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (UI wiring, browser-verified) |
| Graph terminology clarified: the AI-extracted concept/entity graph is now consistently labeled "AI Knowledge Graph" everywhere (Explorer's Reconcile button, Interactive Graph panel, Graph Intelligence panel), and the separate documents/notes/highlights/tags relationship graph — previously also just called "Knowledge Graph," a genuine naming collision — is now "Content Connections." Both systems are unchanged; only labels and docs moved | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | ✅ | — |
| Shared source-reference resolution (`src/modules/knowledge-intelligence/api/sourceResolution.ts`: `fetchTitlesByIds` + `resolveSourceItems`) extracted from three near-identical implementations in `getKnowledgeNodeEvidence` (Evidence), `listCollectionItems` (Collections), and `useKnowledgeNodeDetails` (Explorer/Dashboard) — same skip-if-unresolved behavior everywhere now enforced by one function instead of three copies of it | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ |
| Collection membership error handling: a failed items fetch on a Collection's detail page previously fell back to `[]` and rendered "Nothing in this collection yet" — indistinguishable from a genuinely empty collection. Now shows a loading spinner while fetching, a distinct error state with a Retry button on failure, and the same for `AddToCollectionButton`'s reverse "which collections is this item in" query | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (UI wiring, browser-verified) |

### Audit findings from this sprint (not fixed — out of scope or pre-existing)

- **The Content Connections graph was completely undocumented before this sprint** — Chapter 4 of the Manual only ever covered the AI Knowledge Graph, and `docs/feature-matrix.md` had no row for the other graph at all, despite it existing since Phase 6C. Fixed as part of the terminology-clarification pass (new Manual section, new feature-matrix row) since it was the direct cause of the naming collision this sprint was asked to resolve.
- **`AddToCollectionButton`'s "Added" indicator can go stale if the membership fetch fails**: the dropdown's per-collection "Added" label derives from the same `membership` query the new error banner covers, but the dropdown itself isn't disabled during an error — a user could click "Add" on a collection the item is actually already in (silently a harmless no-op via the existing `knowledge_links` insert) rather than seeing it pre-marked "Added". Flagged, not fixed: disabling the dropdown during a membership error is a small additional behavior change beyond "add loading/error/retry," so it was left for a follow-up rather than expanding scope mid-sprint.
- **Content Connections' own graph query threw a client-side error in browser testing** (`Cannot read properties of undefined (reading 'map')`, surfaced via the page's existing `isError` handling — the error state itself displayed correctly). This traces into `useKnowledgeGraph`'s composition of `listDocuments`/`listNotes`/`listRecentHighlights`/`listTags`/`listKnowledgeLinks`, none of which this sprint touched — it may be a gap in the test harness's mock fixtures (highlights/tags tables weren't fully seeded) or a genuine pre-existing bug under some data condition. **Resolved by the Reliability & Truth Audit**: reproduced deliberately (mock omitting `document_tags` on a document row → error; mock including `document_tags: []` → no error). Real PostgREST always returns an array for a nested embed, so this is confirmed as a test-harness fidelity gap, not a live defect. No code change needed.

## Reliability & Truth Audit

Not a feature phase — a full-platform verification pass across every implemented capability, run against both a mocked backend (interaction correctness) and, for the first time, the live production Supabase project directly (real rows, real errors, real logs). Full report: `docs/reliability-truth-audit.md`. Five confirmed defects found and fixed; one deployment-gap finding (not a code defect) identified as the dominant blocker to acceptance; two items left open pending a human decision rather than more code.

| Fix | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| AI Workspace Actions NL matcher widened to accept real production phrasings (polite lead-ins, trailing "to/in (my) notes") found by querying live user messages — the v1 exact-phrase pattern would have missed them even if deployed | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ |
| Merge Notes: added loading state and a specific, non-overclaiming error message — previously a failed merge produced zero user-facing feedback | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (UI wiring, verified via tsc/vitest/build) |
| `useKnowledgeNodeDetails`'s existing-but-unreturned `isError` now exposed with a `refetch`; Explorer and Dashboard insights both now render a distinct error state with Retry instead of silently falling through to "empty" | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (UI wiring, verified via tsc/vitest/build) |
| `InsightPanel` extended with optional, backward-compatible `isError`/`onRetry`/`errorMessage` props to support the above | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — |
| Knowledge Confidence source-count discrepancy (see note above) fixed — `useKnowledgeNodeDetails` now counts only resolved sources | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (existing coverage) |

### Findings that are not code defects

- **Deployment gap** — `main` is 11 commits behind this branch and contains zero files under `src/modules/workspace-actions/`; none of Knowledge Confidence, Knowledge Actions, Knowledge Collections, Natural Language Commands, AI Workspace Actions, or either Coherence sprint has reached production. This is the direct, confirmed explanation for the user's real observation that Save-to-Notes "doesn't work" in the deployed app — it was never running there. See Recommendation in the full report.
- **Real production `ai_requests` errors** (missing provider API keys, low Anthropic credit balance, occasional edge-function non-2xx) reviewed directly. Classified as configuration/operational, not code defects — the already-accepted Provider Availability Detection and Fallback Chain features exist to handle exactly this, and this data confirms they're surfacing it correctly in production.
- **One historical note** (`3bb041d4-...`) is missing its embedding — root-caused to a real gap in `SaveConversationDialog` that predated the `095aed9` fix already on this branch. The fix prevents recurrence once deployed; the one existing affected row is a data-cleanup decision, not a code fix, and is left open pending the user's call on whether to backfill it.
- Security/performance advisories from Supabase (mutable `search_path`, RLS policies re-evaluating `auth.*()` per row, leaked-password protection disabled, etc.) were reviewed and are pre-existing infrastructure items unrelated to the audited features — listed as a follow-up recommendation in the full report, not fixed here.
- **Knowledge Confidence's source-count accounting differed between `useKnowledgeNodeDetails` and `getKnowledgeNodeEvidence`**: the Explorer/Dashboard path counted every `knowledge_node_sources` row toward confidence, including ones whose title can't be resolved (e.g. a deleted document); the Evidence/drill-down path only counts resolved evidence. Predated this sprint, surfaced while extracting `resolveSourceItems`, deliberately left unchanged here since a behavior change wasn't part of "no behavior changes" for this sprint specifically. **Fixed in the Reliability & Truth Audit** (see `docs/reliability-truth-audit.md`) — `useKnowledgeNodeDetails` now counts only resolved sources, matching the other path exactly. A second, separate divergence in the same confidence inputs (`relatedConceptCount`, capped at 200 edges on Explorer/Dashboard vs. an unbounded per-node query on Evidence/drill-down) was found in that same audit and is documented there as intentionally deferred, since fixing it needs new query infrastructure.

## UX-14: Intelligence Operating System Evolution

Not a Knowledge Intelligence initiative phase — the platform's next evolution stage, from a workspace users operate into a system that works alongside them. Preceded by three planning documents (no code): `docs/ux-14-strategic-roadmap.md`, `docs/ux-14-engineering-blueprint.md`, `docs/ux-14-architecture-consolidation.md`. Implementation proceeds as small, individually verified milestones — each ⚙️ Implemented here reflects code merged to `main`, not yet user-verified.

| Milestone | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| UX-14.1 — Recommendation Consolidation: `suggestionEngine.ts` + `dashboardRecommendations.ts` merged into one `recommendationEngine.ts` (one `Recommendation` type, one `generateRecommendations({scope, ...})` function), behavior-preserving for both existing consumers | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ |
| UX-14.2 — Planner Integration: `buildReasoningPlan` moved from `ChatPage.tsx` (post-response, UI-only) into `AIService.sendMessage` (pre-LLM-call); carried through `SendMessageResult`, not yet injected into the prompt | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ |
| UX-14.3 — Memory Intelligence, Phase 1 (Confidence Persistence): `ai_memory.confidence` persisted (additive migration `0027_ai_memory_confidence.sql`), the exact discard point at `MemoryManagementPage.tsx`'s `rememberCandidate` closed, retrieval/ranking/prompt text unchanged | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (no new test file; API-layer pass-through, verified via tsc + live-DB transaction check, see `docs/ux-14-architecture-consolidation.md`) |
| UX-14.3.5 — Memory Capture Pipeline (Discovery + Activation): `detectMemoryCandidates` wired into `useSendMessage` (shared by ChatPage/ReaderChatPanel), `MemoryApprovalPanel` mounted on both surfaces, `rememberCandidate` consolidated into one shared `useMemories()` function used by all three call sites; no automatic saving, no detection/prompt/model changes | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (no new test file; thin wiring over already-tested `detectMemoryCandidates`, verified via tsc + boot-smoke, see `docs/ux-14-architecture-consolidation.md`) |
| UX-14.4 Phase 1 — Artifact Intelligence Foundation: new `ArtifactKind` registry + deterministic `detectArtifactKind` classifier, tagged into `generation_metadata` by `saveMessageToNote`/`generateBriefing`, a Save As kind picker in `SaveConversationDialog`, an `ArtifactKindBadge` on `NoteCard`, and `KnowledgeCard` (Phase 7B, previously unused) activated on `NoteDetailPage` for the `knowledge_card` kind; no exports, no new schema, no collaboration | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (19 new tests: `detectArtifactKind`, `artifactMetadata`) |
| UX-14.4 Phase 2 (Path A) — Deterministic Spreadsheet Artifact Export: markdown-table → cell parser, explicit formula cells preserved and never evaluated, `generation_metadata.artifactData` payload, format-agnostic XLSX/CSV export dispatcher, preview + Download action on `NoteDetailPage`, lazy-loaded to keep `xlsx` out of the main bundle; no AI generation (Path B), no formula engine, no PDF/DOCX, no new schema, no collaboration | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (20 new tests, incl. a real `XLSX.write`/`XLSX.read` round trip catching a formula-drop bug before commit) |
| UX-14.4.5 — Artifact Acceptance Harness: lightweight code-level acceptance path (not a live Supabase/browser harness — explicitly not built, see doc) — a full-pipeline lifecycle integration test, named regression tests for both Phase 2 discoveries (SheetJS formula-drop, eager-`xlsx`-import), and a `npm run verify:bundle` script checking real `vite build` output; zero application code changed | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (5 new tests; bundle script independently verified to both pass on real output and fail on a simulated regression) |
| UX-14.4 Path B — Artifact Intelligence Generation Discovery: design-only. Maps natural-language spreadsheet generation onto the existing Workspace Action Router (found, not built new) + `runCapability` governance; identifies a real formula-injection risk requiring a new validation allowlist before any LLM-authored formula reaches `buildSpreadsheetWorkbook`; recommends a propose-then-approve consent model (departing from Generate Briefing's auto-save) for generated artifacts | 📝 discovery only | claude/pip-edge-function-deploy-9lzs8n | n/a | n/a | n/a (no code) |
| UX-14.4 Path B — Spreadsheet Specification Contract Discovery: design-only. Proposes a new row/column-oriented `SpreadsheetSpecification` type (distinct from `SpreadsheetArtifactData`) so a future producer never has to compute A1 cell addresses; a `validateSpreadsheetSpecification`/`compileSpreadsheetSpecification` split with a reject-whole-spec failure mode; `SpreadsheetArtifactData`/`buildSpreadsheetWorkbook` left completely unchanged, Path A's parser left as a separate parallel producer | 📝 discovery only | claude/pip-edge-function-deploy-9lzs8n | n/a | n/a | n/a (no code) |
| UX-14.4 Phase 3 — Spreadsheet Specification Compiler: implements the approved specification contract — `specificationTypes.ts`, `validateSpreadsheetSpecification` (typed errors, reject-whole-spec, size caps), `compileSpreadsheetSpecification` (pure, deterministic, O(rows×columns), same-row `{{ColumnName}}` formula placeholder resolution); `SpreadsheetArtifactData`/`buildSpreadsheetWorkbook` unchanged except one additive optional `columnFormats` field; cross-verified byte-equivalent to Path A's markdown parser for the same table; no AI, no Workspace Action, no formula engine, no DB migration | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (28 new tests, incl. the Path A cross-verification test — the milestone's primary correctness guarantee) |
| UX-14.4.1 — Intelligence Foundation Hardening: Path B preparation, not Path B itself. Extracted the previously-duplicated `columnLetter` into shared `cellAddressing.ts` (Path A parser + Phase 3 compiler both now import it, zero behavior change); built `validateFormulaSafety.ts`, a formula-content safety allowlist (cell references + `SUM`/`AVERAGE`/`MIN`/`MAX`/`COUNT` only; rejects `WEBSERVICE`/`HYPERLINK`/`IMPORTXML`/DDE-style payloads) as a standalone module, not yet wired into any pipeline; no AI generation, no Workspace Action, `buildSpreadsheetWorkbook` and both existing producers behaviorally unchanged | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (16 new tests: 3 for `columnLetter`, 13 for `validateFormulaSafety` incl. every named injection payload) |
| UX-14.4.2 — Spreadsheet Intelligence Generation: Path B's actual LLM integration. New `generateSpreadsheetArtifactAction` on the existing Workspace Action Router ("create a spreadsheet for X", deterministic command matching, no parallel intent system); new `generate-spreadsheet-artifact` capability (own `module.ts`) whose prompt requires a `SpreadsheetSpecification` JSON object, never raw XLSX/`SpreadsheetArtifactData`; full pipeline `parseSpreadsheetSpecificationResponse` (tolerant JSON extraction) → `validateSpreadsheetSpecification` → `compileSpreadsheetSpecification` → `validateFormulaSafety` (its first real caller, fail-closed) → `renderSpreadsheetArtifactMarkdown` → chat reply; response is an unsaved preview — no `createNote` call, existing Save-to-Notes flow is the only persistence gate, unmodified; a real bundle regression (reusing `sheetToGrid.ts` pulled `xlsx` into the eager Workspace Action bundle) found via `verify:bundle` and fixed with a new dependency-free `decodeCellAddress`, now permanently regression-tested | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (36 new tests: command matching, tolerant JSON parsing, markdown rendering + render→parse round trip, `decodeCellAddress`, and end-to-end action wiring incl. validation-failure and formula-safety-rejection paths) |
| UX-14.4.3 — Intelligence Experience Layer: the three approved discovery items only. (1) Structured Artifact Preview — additive, artifact-agnostic `ArtifactPreview` on `WorkspaceActionOutcome`, threaded through `AIService`/`useSendMessage` to the one matching `MessageBubble`, which renders a new `ArtifactPreviewCard` (reuses `KnowledgeCard`, no new component framework, no fabricated confidence) instead of plain markdown for that turn; `responseText`/Save-to-Notes pipeline byte-for-byte unchanged. (2) Artifact provenance — `ArtifactCreationMethod` (`user_created`/`conversation_saved`/`ai_generated`/`imported`/`derived`) via `withCreationMethod`/`getCreationMethod`, same additive `generation_metadata` pattern as `withArtifactKind`, wired into all 5 `createNote` call sites, no migration. (3) Knowledge relationships — `linkNoteToMemory` primitive on the existing `knowledge_links` table (zero schema change, confirmed no CHECK constraint on `target_type` anywhere); automatic memory matching deliberately not built (no existing algorithm transfers, documented as future work). Critical regression test proves generate → preview → save → note still retains `artifactKind`/`artifactData`/`creationMethod` correctly, fully unmocked except at the two existing network/DB boundaries | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (10 new tests: `getCreationMethod`/`withCreationMethod`, `linkNoteToMemory`, and the end-to-end generation→save lifecycle regression) |
| UX-14.4.4 — Artifact Intelligence Polish: discovery-first, traced the real `ArtifactPreview` flow end to end and found one genuine regression plus two write/read asymmetries, fixed all three, added nothing speculative. (1) A real bug: `MessageBubble` renders `ArtifactPreviewCard` *instead of* `message.content` (UX-14.4.3), silently dropping the "hasn't been saved yet — say 'save this'" framing for every generated spreadsheet since — fixed with an explicit `StatusBadge` "Preview — not saved" on the card. (2) `creationMethod` (UX-14.4.3) was written at every `createNote` call site but read by nothing — fixed with `ARTIFACT_CREATION_METHOD_LABELS` + a provenance line on `NoteDetailPage.tsx`. (3) `linkKnownConceptsToSource` has recorded a note's related knowledge since Phase 2B, but only ever queryable node-first — fixed with `getRelatedKnowledgeForNote` (the exact reverse of the existing `getKnowledgeNodeEvidence` query, same table) + `useRelatedKnowledge` hook + the existing `SourceReference` chip primitive on `NoteDetailPage.tsx`. No fabricated confidence, no automatic memory matching, no "Improve" capability, no schema changes | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (2 new tests: `getRelatedKnowledgeForNote`) |
| UX-14.5 — Collaboration Infrastructure discovery (4 passes, no code): `ux-14.5-knowledge-exchange-collaboration-discovery.md` surveyed the collaboration space and recommended workspace membership as the target model; `ux-14.5.2-identity-workspace-permission-architecture.md` designed the target workspace/membership/role/RLS mechanics (one reusable `security definer` helper function, per-command RLS reusing the `ai_requests` precedent); `ux-14.5.3-intelligence-object-access-model.md` found, by reading the actual retrieval code, that a uniform RLS rollout would silently leak `ai_memory` (personal profile/preference/conversation memories — `retrieveMemoryContext`/`listMemories` have no explicit user filter of their own, relying entirely on RLS) into shared-workspace chat context, and that `match_document_chunks` has its own hard-coded `user_id` filter independent of table RLS, requiring its own fix for shared-document retrieval to work at all; `ux-14.5.4-collaboration-implementation-blueprint.md` resolves all three into a final object access matrix, data model proposal, RLS migration strategy (two categories, not one universal pattern), AI retrieval boundary design, a Workspace Action permission-gate design, and a 4-phase implementation roadmap starting with the membership table + helper function proven on `notes` alone | 📝 discovery only | claude/pip-edge-function-deploy-9lzs8n | n/a | n/a | n/a (no code) |
| UX-14.5 Phase 1 — Identity Foundation: the first real UX-14.5 code, deliberately narrow. New migration `0028_workspace_members.sql` (applied to the live project): `workspace_members` table (`role`: owner/editor/viewer, `status`: active/pending/removed, unique per workspace/user pair, at most one active owner via a partial unique index), a new trigger auto-creating the creator's explicit owner row for every workspace created from this migration forward (mirrors `handle_new_user`'s existing auto-provisioning pattern), and `has_workspace_role(workspace_id, minimum_role)` — a `security definer` SQL helper implementing a two-tier role check (explicit active membership row, falling back to `workspaces.user_id` as the implicit owner) so every pre-existing workspace resolves to exactly its current single-owner behavior with zero backfill. `resolveWorkspaceRole()` is the identical TypeScript mirror, the one client-side source of truth. `workspace_members`'s own RLS is the first (and, this phase, only) consumer of the helper — `notes`/`documents`/`conversations`/`ai_memory`/the knowledge graph and every AI retrieval path are completely untouched, confirmed live via `pg_policies` showing `notes`/`documents` byte-identical to before. No sharing UI, no invitations, no note/document sharing — those are later, separately-approved phases | ⚙️ | main | ❌ | — | ✅ (11 new tests: `resolveWorkspaceRole` incl. the implicit-owner-fallback and non-member cases, `createWorkspaceMembership` incl. duplicate-membership rejection; plus live-database verification against the production project — trigger firing, unique-constraint rejection, and all three roles' `has_workspace_role` results confirmed directly, see `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §11) |
| UX-14.5 Phase 2 — Note Sharing Design discovery (`ux-14.5.5-note-sharing-design.md`, no code, no RLS applied): recommends extending `notes`' existing RLS (plus `note_tags`/`note_embeddings`) with `has_workspace_role`, per-command, over a dedicated `note_permissions` table — the brief's own object chain and "no generic permissions framework" constraint rule out the latter directly. Target policy: select at viewer-or-above, update at editor-or-above, delete at the resolved *workspace owner* specifically (not editor), insert unchanged (self-attributed, no membership check needed). Traced the actual code and found two real, concrete requirements: (1) `note_tags`/`note_embeddings`'s own EXISTS policies check `notes.user_id` directly and need the identical extension, or a shared note's tags/embedding stay invisible to a teammate even though the note itself is visible — the same structural gap Doc 3 found with `match_document_chunks`, independently confirmed here; (2) a real, pre-existing latent bug in `useNote.ts`: `linkKnownConceptsToSource` is called with the note's original owner's id rather than the acting user's, which will silently fail RLS (and silently swallow that failure) the first time a non-owner editor saves a shared note. Confirmed zero overlap with `retrieveContext`/`retrieveGraphContext`/`retrieveMemoryContext` — note sharing cannot affect `ai_memory` or private retrieval context. Awaiting approval; not implemented | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5 Phase 2 — Note Sharing Foundation (implementation, per the design above): new migration `0029_note_sharing.sql` (applied to the live project) splits `notes`' single RLS policy into four per-command policies using Phase 1's `has_workspace_role` — select at viewer-or-above, update at editor-or-above, delete requires the resolved workspace *owner* specifically (insert unchanged, self-attributed) — and extends `note_tags`/`note_embeddings`'s own EXISTS-based policies through the parent note's `workspace_id` the identical way, closing the propagation gap the discovery found. One policy name exceeded Postgres's 63-char identifier limit and was silently truncated on creation — caught by inspecting `pg_policies` after applying, not assumed correct, and fixed both live and in the migration file. Fixed the discovered `linkKnownConceptsToSource` attribution bug in `useNote.ts` (both `save` and `summarize`): now attributes to the acting user (`user.id` via `useAuth()`) instead of `note.user_id`, so a non-owner editor's saves no longer silently fail to feed the knowledge graph. `NoteDetailPage.tsx` now resolves role via a new `useWorkspaceRole` hook (the first UI consumer of Phase 1's `resolveWorkspaceRole`) and gates Save/Summarize (editor-or-above) and Delete (owner-or-above) exactly to the RLS table, showing a view-only notice otherwise — no sharing dialog, invitations, or notifications. Live-verified against production with two throwaway accounts (editor, viewer) plus a non-member, using genuine RLS enforcement (not just catalog inspection): editor can select/update but not delete a shared note; viewer can select only; non-member sees nothing; both child tables correctly follow the parent note for editor writes and viewer reads; a workspace owner can delete a note an editor authored, confirming "full control." All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (1 new test — the first hook-rendering test in this codebase, via a newly added `@testing-library/react` devDependency — asserting the attribution fix's exact behavior; plus 146 files/1107 tests overall, tsc/lint/build all clean, and the live-DB security matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §13.4) |
| UX-14.5 Phase 3 — Workspace Sharing (invitations + member management): new migration `0030_workspace_membership_management.sql` (applied to the live project) closes two gaps found while building this on top of Phase 1/2, not assumed away — `workspaces`' own RLS was still `auth.uid() = user_id` only (Phase 1 deliberately deferred it), so a member added via `workspace_members` still couldn't see the workspace itself, same shape of gap Phase 2 found for `notes`; and `profiles` RLS is intentionally self-only, so there was no way for an owner to resolve "invite bob@example.com" to a `user_id` at all. `workspaces` gets the same four-per-command-policy treatment as `notes` (select extends to viewer-or-above plus a narrower pending-invitee clause since `has_workspace_role` only resolves active rows; update/delete extend to `has_workspace_role(id,'owner')`; insert unchanged). Three new security-definer RPCs, all re-checking authorization explicitly in the function body since running as security definer bypasses table RLS: `invite_to_workspace` (owner-only, rejects granting `role='owner'`, upserts so re-inviting a declined/removed person works without hitting the unique constraint), `respond_to_workspace_invitation` (self-only accept/decline, never touches `role` — sidesteps the privilege-escalation surface a plain self-UPDATE policy would need extra guarding against), `list_workspace_members` (bridges `profiles`' self-only RLS to render who's who, synthesizes the implicit owner row for pre-Phase-1 workspaces with no explicit membership row, so a single-user workspace's roster is never empty). New `WorkspaceMembersPage.tsx` (`/settings/workspaces/:workspaceId/members`) for member administration — viewing open to all members, invite/role-change/remove gated to `role==='owner'` via Phase 2's `useWorkspaceRole` hook, a member's own row never self-actionable. `WorkspaceCard.tsx` now gates Rename/Archive/Delete/Move (all owner-only under the new RLS, including the shared `sort_order` column) to owners, while Open/Set as default/Manage members stay available to every member — the first real UI-permission-gating pass across an existing surface built before roles existed. `WorkspaceManagementPage.tsx` gained a "Pending invitations" section with Accept/Decline. Live-verified against production with two throwaway accounts and genuine RLS enforcement (role/JWT simulation, not catalog inspection): full invite → pending-visibility → accept → active-access lifecycle; non-owner cannot invite or mutate the workspace; re-invite-while-active, invite-as-owner, and double-accept are all correctly rejected by the RPCs; decline hard-deletes and a subsequent re-invite succeeds; non-member sees nothing; existing Phase 1 role-change/removal policies still work unchanged; a removed member's access is verifiably gone. All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (11 new API-layer tests covering every new function including the RPC error paths; 146 files/1118 tests overall, tsc/lint/build all clean, and the live-DB security matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §14.3. No browser walkthrough — this environment has no login credentials for the one real account, noted rather than glossed over) |
| UX-14.5 Phase 4 — Shared Knowledge Objects: new migration `0031_shared_knowledge_objects.sql` (applied to the live project) extends the `has_workspace_role` OR-clause to every object the discovery's access matrix classifies as shareable — `documents`, `document_chunks`, `knowledge_nodes`, `knowledge_links`, `conversations`, `assets` (uses `owner_id`), `knowledge_collections`, plus `workspace_objectives` (the matrix's own explicit reclassification into the shareable bucket) — using the identical four-per-command split `notes` got in Phase 2, plus five child tables with no `workspace_id` of their own (`embeddings`, `document_tags`, `messages`, `message_embeddings`, `knowledge_node_sources`) routed through their parent via `EXISTS`, exactly like `note_tags`/`note_embeddings`. Closed the retrieval-layer gap the discovery flagged in the same unit of work: `match_document_chunks`/`match_messages` each had a hard-coded `user_id = filter_user_id` clause independent of table RLS that only ever matched the caller's own rows regardless of `filter_workspace_id` — fixed by checking the row's own `workspace_id` against `has_workspace_role` directly, which also correctly covers document-scoped chat (which deliberately omits `filter_workspace_id`). `retrieveGraphContext` needed no fix at all — confirmed by reading it, not assumed — it already had no explicit user/workspace filter and inherits correctness once its four underlying tables' RLS extended. A write-attribution audit (the same class of check that found Phase 2's `useNote.ts` bug) traced all 8 relevant INSERT/UPSERT call sites and found every one already attributes to the acting user — no companion TypeScript fix was needed this phase. A separate audit of all 7 relevant list-view APIs found none apply a redundant client-side `user_id` filter — all defer entirely to RLS, so shared content surfaces in every list view with zero application-layer changes; one stale RLS-describing code comment (`knowledge-graph/api/graph.ts`) was found and corrected in passing. Live-verified against production with a full object set (document, chunk, embedding, knowledge node/link, collection, objective, conversation, message, message embedding, asset) and two throwaway accounts: editor sees all 10 object types via workspace membership; `match_document_chunks` now correctly returns the owner's chunk to an editor both with and without `filter_workspace_id` set (previously would have returned zero either way); `match_messages` works the same for a viewer; non-member gets zero rows at the retrieval layer too; viewer cannot mutate; and critically, an editor querying an `ai_memory` row explicitly tagged with the *same shared workspace's* `workspace_id` still sees nothing — proving the personal-intelligence exclusion is enforced by policy, not by rows happening to lack a workspace_id. All test data cleaned up (including orphaned rows from this app's `workspace_id` FKs being `on delete set null`, not cascade). Deliberately out of scope: `collections` (library folders, distinct from `knowledge_collections`), `chapter_summaries`/`flashcards`/`highlights`/`extraction_metadata`/`processing_jobs`/`tags` (residual gap, not a security exclusion), and any UI permission-gating pass across the newly-shareable objects' own pages (left for the "Collaboration Experience" phase) | ⚙️ | main | ❌ | — | ✅ (146 files/1118 tests, unchanged — no new TypeScript logic this phase, only RLS/SQL and one comment fix — tsc/lint/build all clean, live-DB security + retrieval matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §15.4) |
| UX-14.5 Phase 5 — Collaboration Experience: the UI-layer counterpart to Phase 4, no schema/RLS/RPC change at all — every control this phase adds reads data Phases 1–4 already made visible or mirrors a role check that was already enforced server-side but had no UI. Discovery confirmed role gating existed only on `NoteDetailPage.tsx`/`WorkspaceCard.tsx`; every other shareable object's card/detail page rendered Rename/Delete/Reprocess/Archive unconditionally, and nothing showed who else was in a shared workspace. New primitives in `shared/components/collaboration/` (none of this kind existed before): `getInitials` + `MemberAvatar`/`MemberAvatarStack` (initials-on-a-circle, no photo support anywhere in the app), `SharingBadge` (wraps the existing `StatusBadge` rather than duplicating it), `OwnershipLine` ("Owned/Shared by X," returns `null` until data resolves). New `useWorkspaceMemberDirectory(workspaceId)` hook wraps Phase 3's `list_workspace_members` RPC — zero new backend surface — adding `lookup(userId)` and an `isShared` computed from active-member count (`>1`), not `workspace_id !== null`, so a never-shared workspace still reads "Personal." Applied `canEdit`/`canDelete` gating (mirroring each object's own RLS split exactly) to `NoteCard`/`DocumentCard`/`ImageCard`/`ConversationList` (the last required extracting its `renderRow` function into a real `ConversationRow` component so hooks could legally run per-row) plus `SharingBadge`/`OwnershipLine` on each; and to the four detail pages that had zero gating before this phase — `DocumentDetailPage`, `ImageReaderPage`, `KnowledgeCollectionDetailPage` (`SectionHeader`'s `title` prop widened `string`→`ReactNode`, a strict superset, to fit a badge inline), and `ChatPage` (title/provider-switch controls disabled for non-editors; also where "Last edited by" — deliverable #3 — landed, scoped to conversations only via `messages.user_id`, the one table in the whole schema with genuine per-turn attribution; every other object type was checked and has no `updated_by`-shaped column). `WorkspaceMembersPage` gained per-row avatars; `WorkspaceCard` gained a member-avatar stack; `WorkspaceIntelligenceHubPage` gained a new "Collaboration" section (members, shared note/document/collection counts, merged recent-activity feed) built entirely from data already fetched elsewhere (`useWorkspaceMemberDirectory`, the existing `useWorkspaceStats` — widened to accept a nullable workspace id with an `enabled` gate — and the Hub's own `recentNotes`/`activeConversations`), rendered only when the workspace is actually shared. `DocumentTagEditor`/`CollectionMoveSelect` deliberately left ungated, matching Phase 2's explicit `NoteTagEditor` precedent. Live-verified against production with a throwaway owner+editor workspace: `list_workspace_members` returns the right shape for `useWorkspaceMemberDirectory` to consume; `has_workspace_role` for the editor resolves viewer-true/editor-true/owner-false, matching every `canEdit`/`canDelete` computation added this phase. All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (147 files/1123 tests — 5 new `getInitials` tests are the only new test surface, everything else is presentational gating verified live; tsc/lint/build all clean, live-DB verification matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §16.6) |
| UX-14.5 Phase 6 — Knowledge Exchange discovery (`ux-14.5.6-knowledge-exchange-discovery.md`, no code, no schema/RLS changes): frames Knowledge Exchange as a fundamentally different problem from Phases 1–5's live in-workspace collaboration — moving intelligence *across* the RLS boundary to an account that may never have had access, which no RLS OR-clause can safely express. Classifies all 12 brief-named object types plus 2 the audit surfaced (`extraction_metadata`, `tags`) against the live schema: Notes/Knowledge Collections/Knowledge Nodes/Knowledge Links transferable outright; Documents/Assets transferable but flagged with a real, unsolved cross-account storage-copy gap (confirmed: no such code exists anywhere); Flashcards/Highlights/Chapter Summaries optionally transferable, document-attached only, since their RLS was never extended even intra-workspace (Phase 4 §15.6's "residual gap"); Conversations transferable only via the existing "save to note" conversion pattern, not as a raw export; Reading progress never transferable; no `attachments` object exists. Recommends **immutable snapshots** over live references or a hybrid model — the only option requiring zero RLS change, matching the one real precedent already in the codebase (the client-side-only "Export Knowledge Package" markdown download from Knowledge Actions v1, which has no import counterpart today). Import model: importer always becomes new owner, workspace assignment always explicit, source ids never reused, and duplicate detection scoped narrowly — no dedup for freeform content, but real reuse of Phase 9A's `resolveCanonicalNode` resolver for Knowledge Nodes, flagging that its known accepted-but-unenforced race-condition trade-off becomes a real risk under concurrent multi-account import for the first time. Confirms `ai_memory`/`provider_overrides`/confidence history/`ai_requests`/`reading_progress` must never leave an account (extending §3.3 of `ux-14.5.3-intelligence-object-access-model.md` across the harder account boundary) — and catches a real discrepancy in the brief's own framing by checking the live migration rather than assuming: `workspace_objectives` was already reclassified as *shareable* by `0031_shared_knowledge_objects.sql`, so this discovery recommends treating it like `notes`, not silently folding it into the personal bucket. Maps the brief's 8-stage lifecycle onto existing machinery (package construction reuses `listCollectionItems`; integration reuses the existing embedding/dedup/search-indexing/concept-linking pipeline from the `generateBriefing` precedent — "Knowledge Graph Update" is a consequence of that, not a separate mechanism to build). Roadmap: smallest slice is same-instance, single-collection, Notes-only export→import (6A, zero new tables/RLS); phased after that are Documents/Assets with real cross-account storage copy (6B, flagged as needing a dedicated security review) and cross-instance file-based import with untrusted-input validation (6D, the second security-review-worthy component). Do Not Build Yet: live/hybrid semantics, a generic attachments abstraction, fuzzy content dedup, a package marketplace or public sharing, real-time sync, organization-level exchange policies. **Not yet approved for implementation** | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5.7 — Collaboration Command Center: small UX-only polish, no schema/RLS changes. Promotes the Hub's Collaboration section (Phase 5, buried partway down the page, only visible once already shared) into its own destination — new `/collaboration` route (`WorkspaceCollaborationPage.tsx`, current-workspace-scoped like `/hub`) plus a new Sidebar/mobile-nav entry, so it's discoverable independent of the Hub and regardless of sharing state. Consolidates what were three separate places (Hub's inline section, the standalone Settings members page, no dedicated overview at all) into one: full member management (invite/role-change/remove) and shared-intelligence stats + recent activity now live together on one page. Extracted `WorkspaceMembersPage.tsx`'s body into a new `WorkspaceMemberRoster.tsx` component (behavior-preserving refactor) so it's reused by both the new page and the existing per-workspace-id Settings route rather than duplicated; reused `CollaborationSection.tsx` unchanged except removing its now-redundant "Manage members →" link (the full roster sits directly beneath it on the new page). Hub's own Collaboration section trimmed to a lightweight teaser (member count + avatar stack + "Open Collaboration →" link) rather than duplicating the full content in two places. Net change is a code *reduction* (158 lines removed from WorkspaceMembersPage via extraction, 48 net insertions across all 5 modified files) | ⚙️ | main | ❌ | — | ✅ (147 files/1123 tests, unchanged — pure reuse/composition, no new logic to test; tsc/lint/build all clean) |
| UX-14.5.8 — Workspace Invitations discovery (`ux-14.5.8-workspace-invitations-discovery.md`, no code): audits the reported "Failed to send invitation" failure end to end and reproduces it live against production rather than guessing. Root cause: not a bug in the existing mechanism — `invite_to_workspace` (Phase 3) works correctly, live-confirmed, for inviting someone who **already has a NOVA PIP account**; it raises `P0001: No account found for that email` for anyone who doesn't, which is the overwhelmingly common real case for "invite a teammate." A second, live-reproduced bug compounds it: the email match is case-sensitive with no normalization, so even an existing account fails to be found if the inviter types different casing than it was stored. Separately confirms **no email delivery infrastructure exists anywhere in this codebase** (exhaustive grep for Resend/SendGrid/SMTP/etc. — zero hits) — even a *successful* invite sends no notification; the invitee only discovers it by manually checking Settings → Workspaces, which the new Collaboration Command Center (UX-14.5.7) doesn't even surface. Flags a live-confirmed, low-severity email-enumeration side channel: the RPC's distinct error messages let any workspace owner probe whether an arbitrary email has an account. Recommends a 3-tier smallest-first architecture: (10.1) ship immediately — case-insensitive email match, non-generic UI error text, and closing the enumeration side channel, all near-zero-risk; (10.2) the actual gap-closer — a new, additive `workspace_invitations` table keyed by email (not `user_id`, which the schema has no slot for on an unresolved identity) with resolution wired into the existing `handle_new_user` trigger (the same "react to a new auth.users row" pattern this codebase already trusts), converting today's hard failure into a working manual-handoff invite flow with **zero new external dependencies and zero service-role key usage**; (10.3) explicitly deferred — real email delivery via Supabase Auth's `inviteUserByEmail` Admin API, flagged as this app's first-ever service-role-key/edge-function trust boundary (confirmed: neither of the app's two existing edge functions uses one today) and therefore warranting its own dedicated security review before implementation. Live-verified against production with two throwaway accounts: reproduced the primary failure, reproduced the case-sensitivity bug, and confirmed the success path works correctly for an existing account. All test data cleaned up. **Discovery only — no implementation performed** | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5.8 Phase 1 — Invitation Foundation: implements the discovery's smallest recommended slice, approved and scoped tightly (`workspace_invitations` table, invitation creation, pending-invitation display, signup reconciliation, email normalization, generic error handling — explicitly **no email sending**). New migration `0032_workspace_invitations.sql` (applied to the live project): new `workspace_invitations` table (separate from `workspace_members`, which has no slot for an identity that doesn't exist yet — `user_id` is `not null`), RLS-readable by any workspace member with zero client-side write policy (the only writers are security-definer functions). `invite_to_workspace` (Phase 3) redefined — dropped and recreated since its return type changes — to gain a fallback branch: no matching `profiles` row now records a pending `workspace_invitations` row instead of raising `"No account found for that email"`; both branches return success (a small jsonb outcome descriptor), closing the discovery's live-confirmed account-enumeration finding as a side effect of the redesign rather than a separate patch. Email comparison against `profiles` is now case-insensitive, closing the discovery's second live-reproduced bug. `handle_new_user` gained a reconciliation step: every pending, unexpired invitation matching a new signup's (lower-cased) email is promoted into a real `active` `workspace_members` row automatically, reusing the one mechanism this codebase already trusts for "react to a new `auth.users` row." Application layer: `inviteToWorkspace()` normalizes email client-side too (defense in depth); new `listWorkspaceInvitations`/`useWorkspaceInvitations` (a plain RLS-gated select, no RPC needed — each row already carries the invitee's email, no `profiles` cross-user join to bridge); `WorkspaceMemberRoster.tsx` (the component `/collaboration` and Settings both render, UX-14.5.7) gained a read-only "Pending Invitations" list, owner-visible, below the active roster — no cancel/resend/role-change yet (UX-14.5.8.2, out of scope this phase). Live-verified against production end to end: an invite to an unknown, whitespace-and-mixed-case email now succeeds and stores a normalized row; a subsequent signup with that exact email auto-promotes to a real active membership with the invited role, with the invitation correctly marked accepted; the known-account path still works exactly as Phase 3 built it, confirming the redefinition didn't regress it. All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (147 files/1127 tests — 4 new, covering the new outcome shape, email normalization, and `listWorkspaceInvitations`; tsc/lint/build all clean, live-verification matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §18.3) |
| UX-14.5.8.2 — Invitation Management (Cancel + Resend): adds the two owner actions Phase 1 explicitly deferred, still no email sending and no role-change-before-acceptance. New migration `0033_workspace_invitation_management.sql` (applied to the live project): one owner-gated UPDATE RLS policy on `workspace_invitations` for Cancel (`workspace_invitations` shipped in Phase 1 with zero client write policy, on purpose) — the same plain-RLS-UPDATE shape `updateWorkspaceMemberRole`/`removeWorkspaceMember` already use, not the RPC pattern invitation creation needs, since cancelling has no cross-user profile resolution to do. Resend needed **zero schema/backend work** — re-inviting the same email through the existing `invite_to_workspace` RPC already refreshes `role`/`invited_by`/`expires_at`/`updated_at` on the same row via its own Phase 1 `on conflict` clause, so "Resend" in the application layer is just calling that same mutation again with the invitation's own stored email/role. Application layer: new `cancelWorkspaceInvitation()`; `useWorkspaceInvitations` gained `cancel`/`resend` mutations; `WorkspaceMemberRoster.tsx`'s Pending Invitations rows gained Resend/Cancel buttons (owner-only) and a "· Renewed {relative time}" suffix whenever `updated_at` differs from `created_at` (the same Created/Updated pattern `NoteCard.tsx` already uses), so a resend is visibly confirmed without a toast system this app doesn't have; Cancel goes through a `ConfirmDialog` matching the existing Remove-member confirmation. Live-verified against production: a non-owner's cancel attempt is correctly denied by RLS (0 rows, invitation stays pending); the owner's cancel succeeds; re-inviting after cancellation creates a fresh pending row (the partial unique index only blocks a second *pending* row); re-inviting a still-pending invitation updates the *same* row in place (id and `created_at` unchanged, `updated_at`/`expires_at` refreshed) — confirming Resend renews rather than duplicates. All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (147 files/1129 tests — 2 new, covering `cancelWorkspaceInvitation`'s success and RLS-denial paths; tsc/lint/build all clean, live-verification matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §19.3) |
| UX-14.5.8.3 — Email Invitation Delivery: closes the last deferred gap — invitations now actually email the invitee. This app's first service-role-key trust boundary, scoped tightly per the discovery's own recommendation: email delivery only, **zero database/schema changes**. New Edge Function `supabase/functions/send-workspace-invitation/index.ts` (deployed to the live project, `verify_jwt: true`): authenticates the caller via their forwarded session JWT (never anonymous), re-authorizes by calling the *existing* `has_workspace_role(workspace_id,'owner')` RPC through a caller-scoped client (reusing the one authorization primitive this codebase trusts, not reimplementing it), only then escalates to a service-role client and only for the two RLS-blocked reads needed (the invitation/membership row itself, and — for the known-account path — the invitee's email from self-only-RLS `profiles`, the identical reason `list_workspace_members` needed a security-definer function). Re-verifies the row's own `status`/`expires_at` server-side before sending, never trusting the request body. Sends for both invitation shapes symmetrically: the unknown-email `workspace_invitations` path (Phase 1, has an expiry, accept link → `/signup?email=`) and the known-account `workspace_members` pending-row path (Phase 3, no expiry concept, accept link → `/settings/workspaces`, where the existing Accept/Decline UI already lives) — closing the "even a successful invite sends no notification" gap the UX-14.5.8 discovery flagged for both paths, not just the new one. Email provider: Resend via plain `fetch`, no SDK, matching `ai-chat`'s direct-HTTP-provider pattern; `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`SITE_URL` are Supabase Function secrets. **`RESEND_API_KEY` was not set by this implementation** (no tool in this environment can set Supabase secrets) — the user must configure it post-deployment; every other step (auth, authorization, row eligibility, template building) is fully live and fails gracefully with a 500 at the last step without it. Pure eligibility/URL/template logic extracted to `src/modules/workspaces/email/buildInvitationEmail.ts` (dependency-free, tested under `vitest`) and mirrored inline in the edge function itself, matching this codebase's existing precedent of fully self-contained, non-`src/`-importing edge functions. Client layer: `sendWorkspaceInvitationEmail()`/`sendInvitationEmailForResult()`; `useWorkspaceMembers.invite` and `useWorkspaceInvitations.resend` both now run the RPC call then the email send as two separate, non-corrupting steps — a delivery failure surfaces as a distinct `emailError`, never thrown, so `invite.isError` still means specifically "invitation creation failed" and the invitation stays pending, resendable; `WorkspaceMemberRoster.tsx` renders it as a dismissible amber warning separate from the existing red error text. `SignUpPage.tsx` reads `?email=` and prefills (still editable) for the unknown-email accept flow. **Genuine limitation**: this sandboxed environment's network policy blocks outbound HTTPS to `*.supabase.co`, so a full HTTP round-trip against the deployed function (real JWT, real Resend call) could not be performed — live verification instead covered everything reachable through the `execute_sql` MCP tool: `has_workspace_role` for an owner vs. a non-owner, and the exact eligibility/column-shape queries the function depends on, against real rows (pending/expired/accepted invitations, a pending membership joined to `profiles`). Deployment confirmed `ACTIVE`/`verify_jwt: true`; `get_advisors` shows no new findings. All test data cleaned up | ⚙️ | main | ❌ | — | ✅ (148 files/1150 tests — 21 new: 15 for `buildInvitationEmail.ts`'s eligibility/URL/template logic, 6 for `sendWorkspaceInvitationEmail`/`sendInvitationEmailForResult`; tsc/lint/build all clean, live-verification matrix in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §20.3) |
| UX-14.5.9 — Knowledge Exchange Phase 1 (Notes Export/Import MVP): implements the smallest slice `docs/ux-14.5.6-knowledge-exchange-discovery.md` §8.1/§8.2 recommended ("6A"), scoped exactly as approved — **Notes only**, immutable-snapshot semantics, **zero migrations, zero RLS changes, zero workspace-sharing changes**. This is not live collaboration; it's a one-shot, versioned, file-based snapshot transfer. New, isolated package framework under `src/modules/knowledge-exchange/packages/` — `PackageVersion.ts` (`CURRENT_PACKAGE_VERSION`/`SUPPORTED_PACKAGE_VERSIONS`, currently both `1`), `PackageValidator.ts` (`PackageValidationResult<T>` tagged union, never throws), `PackageExporter.ts`/`PackageImporter.ts` (generic `<TSource,TPackage>`/`<TPackage,TResult>` contracts) — the explicit foundation a future Documents/Collections package type plugs into without touching Notes' own code. Concrete Notes implementation under `src/modules/knowledge-exchange/notes/`: `notePackageTypes.ts` defines an **allow-list** package shape (`{version, exportedAt, sourceVersion, note: {title, content, tags, createdAt, updatedAt, generationMetadata}}`) with no field for `id`/`user_id`/`workspace_id`/embeddings/`ai_memory`/provider config — nothing to accidentally forward, by construction, not by a deny-list filter. `exportNotePackage()` is a pure function (note + already-fetched tags already loaded on `NoteDetailPage` → versioned JSON), downloaded via the existing `downloadTextFile` client-side-blob pattern (no server call). `importNotePackage()` always `createNote`s a fresh row (never updates/overwrites), importer becomes owner, workspace is an explicit dialog choice (never inferred from the package), original timestamps travel only inside `generation_metadata.importedFrom` (never as the new row's own `created_at`/`updated_at`), tags re-resolved by name via the existing `ensureTag`-backed `addTagToNote`, then the same `indexNote`/`linkKnownConceptsToSource` integration every other note-creation path in this app already runs — an imported note is indistinguishable at the database level from one created by hand the moment this function returns, inheriting Phases 1–5's entire RLS/collaboration/permission story for free. `validateNotePackage()`/`parseNotePackage()` never trust a package as pre-validated just because it came from this app's own Export — reject missing fields, invalid schema, unsupported version, future version (produced by a newer app build), and malformed JSON, collecting every issue in one pass rather than stopping at the first. New UI: an Export button on `NoteDetailPage`; an Import button on `NotesPage` opening `ImportNotePackageDialog` (built on the same `<dialog>` shell `SaveConversationDialog` established) — file picker → validation summary (friendly messages) or package preview → workspace selector → success summary with a link to the new note. All test data/code paths confirmed via unit + integration tests, not a live browser walkthrough (no login credentials available in this environment, same limitation noted honestly at Phase 3) | ⚙️ | main | ❌ | — | ✅ (152 files/1189 tests — 39 new across `exportNotePackage`, `validateNotePackage`/`parseNotePackage`, `importNotePackage`, and an export→import round-trip integration test covering a cross-account, cross-workspace transfer plus two rejected-before-write negative cases; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §21) |
| UX-14.5.10 — Knowledge Exchange Expansion (discovery, no code): audits whether/how UX-14.5.9's package framework extends to Documents, Knowledge Collections, Knowledge Nodes, Knowledge Links, and Assets, grounded in the actual schema/RLS/API code, not the pre-implementation assumptions of the original Phase 6 discovery doc. **Headline finding**: all five object types already carry the exact ownership/`workspace_id`/RLS shape Notes had, via `0031_shared_knowledge_objects.sql` (UX-14.5 Phase 4) — this task's "no RLS changes" constraint is trivially satisfiable, no authorization work is a prerequisite for any of the five. The framework's four generic files need zero modification for Knowledge Nodes/Documents/Assets as new sibling types following Notes' exact pattern. Confirms one rule generalizes across every type: content mechanically *derived* from source material (Documents' chunks/embeddings/extraction-metadata, Assets' optimized/thumbnail derivatives, Knowledge Nodes' canonical identity) is never copied on import — always regenerated via the exact pipeline function a native create already calls (`processDocument`/`generateDerivatives`/`resolveCanonicalNode`). Identifies two genuinely new problems Notes never faced: binary content (Documents/Assets' storage has no cross-account read path — resolved architecturally by embedding file bytes inside the package itself, sidestepping the cross-account storage-copy infrastructure the original doc assumed was needed) and multi-object orchestration (Knowledge Collections' membership is `knowledge_links` rows, not a join table — a Collection package is a manifest requiring a new, additive orchestration layer). Surfaces one pre-existing, orthogonal gap: `flashcards`/`highlights`/`chapter_summaries` have no `workspace_id` column and were never touched by 0031 — not intra-workspace-shareable today, let alone exportable. Recommends a five-step implementation order (Knowledge Nodes → Assets → Documents → Knowledge Links → Knowledge Collections, each independently shippable) and recommends splitting future implementation into separate phases rather than one pass, starting with Knowledge Nodes only if approved. Deliverable: `docs/ux-14.5.10-knowledge-exchange-expansion-discovery.md`. **Discovery only — no implementation performed** | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5.10.1 — Knowledge Node Exchange Implementation: implements the discovery's own recommended first step (Knowledge Nodes only), following UX-14.5.9's Notes package pattern exactly — the same four-file shape, the same `<dialog>` UI shell, the same allow-list discipline. **Zero migrations, zero RLS changes, zero collaboration changes.** The package framework's four generic files (`PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter`) are confirmed byte-for-byte unchanged — the discovery's own prediction (no framework modification needed for a new single-object type) held exactly. New `src/modules/knowledge-exchange/knowledge-nodes/`: `knowledgeNodePackageTypes.ts` defines an allow-list `{version, exportedAt, sourceVersion, node: {nodeType, title, description, metadata, generationMetadata, createdAt, updatedAt}}` shape with no field for `id`/`user_id`/`workspace_id`/`source_type`/`source_id`/`source_chunk_ids`/`title_normalized` — `knowledge_links` (relationships) explicitly excluded this phase since a link needs both endpoints present, which requires a multi-object package this task's own scope ruled out. `exportKnowledgeNodePackage()` is a pure function (no Supabase call). `importKnowledgeNodePackage()` reuses the existing `resolveCanonicalNode` unmodified (per this task's explicit permission and the discovery's own finding that this requires zero new design work): importer becomes owner, workspace explicit, a fresh `crypto.randomUUID()` `source_id` per call means the resolver's content-mutating `refresh` branch is structurally unreachable for imports — a title match against an existing node lands on `reuse` instead, which leaves the existing node's content completely untouched and only records a new provenance row. This *is* the never-overwrite guarantee and the duplicate-handling behavior, verified directly by testing that a second import of the same package returns `created: false` with the existing node's description unchanged. New source type `knowledge_node_import` is honestly distinct from `document`/`note`/`conversation`; an unrecognized source type is already gracefully dropped by the existing evidence-resolution code (`resolveSourceItems`), confirmed by reading it — no UI change needed there. New UI: an "Export" button on `KnowledgeNodeDetailPage` (distinct from the pre-existing read-only markdown "Export knowledge package" button, kept unchanged) and an "Import" button on `KnowledgeExplorerPage`'s actions row opening `ImportKnowledgeNodePackageDialog` — the exact `ImportNotePackageDialog` shell, adapted only for the create-vs-reuse success message. No sharing links, no collaboration permissions, no Collections integration, no multi-object packages, per this task's explicit scope constraint | ⚙️ | main | ❌ | — | ✅ (156 files/1233 tests — 44 new across `exportKnowledgeNodePackage`, `validateKnowledgeNodePackage`/`parseKnowledgeNodePackage`, `importKnowledgeNodePackage` (including an explicit never-overwrites-on-reuse test), and an export→import round-trip integration test covering a cross-account, cross-workspace transfer, a reuse-not-overwrite second import, and two rejected-before-import negative cases; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §23) |
| UX-14.5.10.2 — Asset Exchange Implementation: implements the discovery's second recommended step (Assets), whose primary purpose was validating the binary-content package strategy before Documents. **Binary packaging decision: Option A — embed the original image's base64-encoded bytes inside the package** (not metadata-only + manual re-upload), chosen for portability (a package stays one self-contained file, matching Notes'/Knowledge Nodes' own invariant), security (the payload never executes — import decodes it into a `File` and routes it through the exact same `uploadAsset` pipeline a manual upload already uses, so embedding bytes adds no new attack surface), file size (bounded by the same `MAX_ASSET_UPLOAD_BYTES` 25MB cap the native upload already enforces, checked pre-decode via a formula-based byte estimate), and existing infrastructure (zero new services — fully client-side). Only the *original* image's bytes ever travel, never `optimized`/`thumbnail` — consistent with this framework's "derived content always regenerates, never copied" rule; `uploadAsset` regenerates both derivatives itself on import. **One justified, minimal framework addition**: two new package-type-local files, `base64.ts` (chunked `uint8ArrayToBase64`/`base64ToUint8Array`, plus a pre-decode `estimateBase64DecodedByteLength` size check) and `assetFileTransfer.ts` (`fetchAssetOriginalAsBase64` — the one place this package type does real I/O, keeping `exportAssetPackage()` itself synchronous like every other package type's exporter); the four generic framework files remain byte-for-byte unchanged. `importAssetPackage()` reuses `uploadAsset` unmodified — always creates a brand-new asset (never dedupes, unlike Knowledge Nodes, since there's no meaningful "same asset" identity) — so ownership, workspace assignment, fresh storage paths, MIME/size validation, and decode-integrity checking all fall out of existing, unmodified code. **Security review**: no arbitrary storage path injection (paths are always freshly generated, never from the package), no signed-URL reuse (the package carries no URL/path at all), no ownership escalation (`owner_id` always `context.userId`), no execution of uploaded content (client-side canvas/Blob decode only; SVG already excluded from `SUPPORTED_IMAGE_MIME_TYPES`), no bypass of existing processing (calls the literal `uploadAsset`). **Trust-boundary finding, directly tested**: the package's declared `mimeType`/`width`/`height`/`sizeBytes` are informational-only (import-dialog preview); the imported row's real values are always independently recomputed from the actual decoded bytes by `generateDerivatives`, so a hand-tampered package lying about its own metadata cannot poison the imported row. **Documented, accepted MVP limitation** (not a workaround): `assets` has no JSONB metadata column at all, so an imported asset cannot record an `importedFrom` provenance block the way Notes/Knowledge Nodes do — flagged in advance by the UX-14.5.10 discovery's own §5, accepted rather than solved via a migration (disallowed this phase). New UI: an "Export" button on `ImageReaderPage`'s details actions block (async handler: fetch signed original → base64-encode → build package → download) and an "Import" button on `LibraryPage`'s Images tab next to the upload dropzone, opening `ImportAssetPackageDialog` — the exact `ImportNotePackageDialog` shell. No bulk exports, no collections, no document packages, no sharing links, per this task's explicit scope constraint. **Zero migrations, zero RLS changes, zero collaboration changes** | ⚙️ | main | ❌ | — | ✅ (162 files/1280 tests — 47 new across `base64`, `assetFileTransfer`, `exportAssetPackage`, `validateAssetPackage`/`parseAssetPackage` (including an oversized-payload boundary test at the exact byte cap), `importAssetPackage`, and an export→import round-trip integration test covering a cross-account/cross-workspace transfer with exact-byte-match assertion, disallowed-MIME/tampered-version/truncated-JSON rejection, and the metadata-is-informational-only trust-boundary case; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §24) |
| UX-14.5.10.3 — Document Exchange Discovery (discovery, no code): a complete implementation blueprint for the third Exchange object type, produced before any code, grounded in the actual documents/document_chunks/document_tags/embeddings/extraction_metadata/processing_jobs/chapter_summaries/highlights/flashcards/reading_progress schema and the full processing pipeline. **Headline findings**: the binary-storage problem is already solved (Documents' bucket has the identical private/per-user-folder RLS shape Assets' bucket does, no new RLS work needed); a previously-undocumented coherence gap — `extraction_metadata`/`processing_jobs`/`chapter_summaries`/`highlights`/`flashcards`/`reading_progress` were never touched by `0031_shared_knowledge_objects.sql` and have no `workspace_id` column, so a workspace member who can already see a shared document cannot see any of its six side-tables at all — confirmed by reading `0031` directly, documented as a real but separate, non-blocking product gap; and the single biggest simplification — `processDocument(documentId, userId)` already regenerates chunks/embeddings/extraction metadata from nothing but the original file bytes, via the identical fire-and-forget call a native upload already makes, meaning none of that AI-derived content ever needs to travel in a package. **Package format decision: Option B — ZIP via the already-present `jszip` dependency** (not a repeat of Assets' base64-in-JSON), chosen because Documents' 200MB cap makes base64 inflation a real browser-memory risk (~700MB-1GB peak for one file) that Assets' 25MB cap never faced; `jszip` needs no new dependency (already used to parse EPUB), gets free per-entry CRC32 integrity, and avoids ~1.33× size inflation — a custom binary format (Option C) was rejected as a portability regression with no existing library backing it. **Object-by-object transfer/regenerate matrix**: `documents`/`document_tags` transfer (the object itself; tags re-resolved by name like Notes); `highlights` transfer (the one genuinely user-authored side-artifact — confirmed by reading `createHighlight`, which never sets the AI-provenance columns `0011_ai_provenance_fields.sql` added — chapter-index-drift risk assessed as bounded/cosmetic and explicitly accepted); `document_chunks`/`embeddings`/`extraction_metadata`/`chapter_summaries`/`flashcards` all regenerate (chapter summaries and flashcards confirmed 100% AI-authored by reading their API files — no manual-authoring code path exists — so regenerating avoids the same chapter-index-drift risk entirely rather than accepting it); `processing_jobs`/`reading_progress` are neither (pure operational/positional state, never applicable to a package). **Processing strategy: immediately enter the existing pipeline** (not "restore directly") — reuses `uploadDocument`+`processDocument` unmodified, zero new pipeline code, gets progress reporting for free via the existing `useProcessingJob`/`ProcessingStatusBadge` polling UI. **Security review**: reuses `uploadDocument`'s existing structural guarantees (fresh IDs/paths, importer-owned, extension allow-list) for everything Assets' review already covered, plus two genuinely new considerations — zip bombs (mitigated by checking each ZIP entry's declared uncompressed size against `MAX_UPLOAD_BYTES` before decompressing, a real must-implement check ZIP introduces that base64-in-JSON never needed) and duplicate documents (no canonical-identity resolver exists, so import always creates a fresh row, matching Notes'/Assets' always-create precedent, not a new risk). **Roadmap**: recommends splitting into three phases — 10.3.1 ZIP package plumbing, 10.3.2 import + processing integration, 10.3.3 highlights added last and separably — unlike prior single-phase object types, given this phase combines binary transfer, a much larger regeneration pipeline, and a new container format. Deliverable: `docs/ux-14.5.10.3-document-exchange-discovery.md`. **Discovery only — no implementation performed** | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5.10.3.1 — ZIP Package Infrastructure: implements exactly the discovery's phase 1 (§9/§25.5) and no more — proves the ZIP container format (export, validation, manifest, CRC, zip-bomb protection) in isolation, deliberately **before** import, per this task's explicit instruction. **No import function, no `uploadDocument`/`processDocument` wiring, no UI at all** — `git status` for this commit touches only new files under `src/modules/knowledge-exchange/documents/` (`documentPackageTypes.ts`, `documentPackageArchive.ts`, `exportDocumentPackage.ts`, `validateDocumentPackage.ts`, plus their tests) and one new top-level `documentPackageRoundTrip.test.ts` — zero existing files modified, confirming no UI/import creep. `buildDocumentPackageZip()` (via the already-present `jszip` dependency, no new dependency) stores the `original` entry uncompressed (STORE) — most document formats are already near-incompressible zip containers themselves, and STORE keeps the archive's outer size trivially predictable, the exact property the zip-bomb size gate depends on. **A genuine discovery-vs-implementation-reality finding**: the discovery assumed jszip exposes an entry's declared uncompressed size as public API; building this phase revealed that field (`JSZipObject._data.uncompressedSize`) is explicitly marked **not yet public** in jszip's own bundled type definitions. Resolved with a defensive read (a future jszip renaming the field fails safe — rejects as unverifiable, never silently skips the check) inside a **two-pass validation design**: pass 1 (`loadAsync` with no options) parses only the central directory — entry names and declared sizes, decompresses nothing — and rejects wrong/missing/extra entries or an oversized declared size **before any decompression happens** (the zip-bomb defense, proven by a real test: a ~200MB all-zero buffer DEFLATE-compressed to under 1MB, rejected on declared size alone); pass 2, reached only once bounded-safe, re-loads with jszip's own public `{checkCRC32: true}` option — genuine, public-API CRC32 verification (proven by a real test: a single byte flipped inside the archive's raw `original` entry data post-export, located via a distinctive content marker, correctly rejected as corrupted) — deliberately not combined with pass 1, which would defeat the zip-bomb defense by eagerly decompressing a potentially-huge entry before its size was ever checked. `parseDocumentPackageZip()` cannot implement the shared, synchronous `PackageValidator<TPackage>` interface (a zip isn't `JSON.parse`-able, reading one is inherently async) — kept as its own compatible-in-spirit async shape rather than changing that interface, which every other package type still correctly relies on being sync; reuses the framework's existing `PackageValidationIssue` codes creatively (`invalid_schema` for "not a valid zip"/wrong-entries/oversized/corrupted, `malformed_json` for the manifest JSON specifically) rather than adding new ones — **the framework itself is unchanged**. `exportDocumentPackage()` stays pure/synchronous (manifest only); the async archive-building work lives in `documentPackageArchive.ts`, mirroring `assetFileTransfer.ts`'s split from UX-14.5.10.2. Manifest allow-list: title/fileName/fileType/sizeBytes/createdAt/tags — no id/user_id/workspace_id/collection_id/file_path/status; highlights deliberately absent, reserved for UX-14.5.10.3.3 | ⚙️ | main | ❌ | — | ✅ (166 files/1307 tests — 27 new; one pre-existing, unrelated flaky test in `exportKnowledgeNodePackage.test.ts` from UX-14.5.10.1, a millisecond-boundary `Date.toISOString()` race, confirmed to pass in isolation and untouched by this commit; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §26) |
| UX-14.5.10.3.2 — Document Import & Processing Integration: implements import directly on §26's proved ZIP infrastructure — no package-format redesign, no framework changes, no migrations, no RLS. **Smallest integration point**: the exact two-call sequence `useDocumentMutations.ts`'s own upload mutation already makes after a native upload — `uploadDocument()` then fire-and-forget `processDocument()` — reused completely unmodified; `importDocumentPackage()` extracts the `original` entry's bytes from the already-CRC-verified `zip` handle `ValidatedDocumentPackage` was deliberately shaped in UX-14.5.10.3.1 to carry forward, reconstructs a `File`, and calls both functions in the same order a native upload does. **A real naming difference from Assets, worked through**: `uploadDocument()` has no separate `title` parameter (unlike `uploadAsset()`) — it derives `title` purely from `file.name` — so the reconstructed File's name is built from the manifest's `title` field (not `fileName`), preserving the exporter's exact title including any in-app rename; the imported row's `file_name` differs cosmetically as an accepted tradeoff, since nothing reads it beyond display. **Regenerated artifact confirmation**: `importDocumentPackage` creates zero rows in `document_chunks`/`embeddings`/`extraction_metadata`/`chapter_summaries`/`flashcards` itself — every one is regenerated exclusively by the single unmodified `processDocument()` call, confirmed by an end-to-end test asserting that mock is invoked with the freshly created document's id and the importer's own userId, and by the importer file never importing anything from the extraction/chunking/embedding modules at all. **Ownership**: importer becomes owner, workspace is the caller's explicit choice, fresh id/storage path always generated by `uploadDocument` (never from the package); tags re-resolved by name via `addTagToDocument`, matching Notes' precedent. **Error handling**: invalid-ZIP/CRC-failure/unsupported-version/oversized-package/invalid-manifest are all rejected by the existing UX-14.5.10.3.1 validator before import is ever attempted (Import button stays disabled until valid); upload failures propagate through the dialog's existing error-surfacing; processing failures are fire-and-forget by design and surfaced via `processDocument`'s own existing `status: 'error'`/`error_message` handling and the "Retry processing" button `DocumentDetailPage` already offers a native upload — no new code needed for either. **No partial imports** falls out of reusing both functions unmodified (`uploadDocument` already cleans up storage on a failed insert; `processDocument` already marks failures `'error'` rather than leaving a document stuck `'processing'`) — the tag-attachment loop keeps Notes' existing best-effort (no-rollback) precedent. New UI: an "Export" button on `DocumentDetailPage.tsx` (async: `fetchDocumentOriginalFile` → `exportDocumentPackage` → `buildDocumentPackageZip` → `downloadBinaryFile`, the first package type whose export needs a binary, not JSON-text, download utility) and an "Import" button on `LibraryPage.tsx`'s Documents tab next to `UploadDropzone`, opening `ImportDocumentPackageDialog` (`.zip` file input, async `parseDocumentPackageZip`) whose success screen reuses `ProcessingStatusBadge`/`useProcessingJob` unmodified — the same live-polling UI a native upload's Library card already shows, no new progress-reporting code. `exportDocumentPackage`'s `tags` parameter was narrowed from `Tag[]` to `Pick<Tag,'name'>[]` (this package type's own file, not the framework) so `DocumentDetailPage` can pass its already-loaded `document.tags` straight through | ⚙️ | main | ❌ | — | ✅ (167 files/1323 tests — 16 new across `importDocumentPackage.test.ts` (ownership, workspace assignment, File reconstruction/extension mapping, tag re-resolution, `processDocument` call ordering, upload-failure propagation, a defensive missing-entry guard) and an extended `documentPackageRoundTrip.test.ts` (full export→import→`processDocument` end-to-end test, plus a CRC-corrupted package and a real DEFLATE zip bomb both asserted to reach neither `uploadDocument` nor `processDocument`); two pre-existing, unrelated flaky tests (one from UX-14.5.10.1, one newly observed in UX-14.5.10.2's `exportAssetPackage.test.ts`, both the same millisecond-boundary `Date.toISOString()` race) confirmed to pass in isolation and untouched by this commit; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §27) |
| UX-14.5.10.4 — Knowledge Link Exchange: implements export/import for the AI-detected node-to-node relationship edges in `knowledge_links` (`source_type`/`target_type` = `'knowledge_node'`), reusing the package framework and the existing canonical node resolver unmodified. **Scope confirmed by audit**: every relationship this app can currently produce is 100% AI-generated (`generated_by` always set — no manual node-to-node linking feature exists), but unlike Documents' chapter summaries/flashcards, a relationship is often the product of cross-document analysis over documents the importer may never have — genuinely exchange-worthy, not a cheaply-regenerable byproduct. `knowledge_links` was already made workspace-shareable by `0031_shared_knowledge_objects.sql` §7 (no RLS work needed); `source_id`/`target_id` are not FK-enforced at the DB level, so "never create broken links" is an application-level discipline this importer upholds itself. **Export contract**: allow-list `{relationshipType, confidence, generatedBy, source: {nodeType, title, description}, target: {nodeType, title, description}}` — no ids, no `workspace_id`, no timestamps; each endpoint is a *minimal descriptor* (exactly what `resolveCanonicalNode` needs), not a full Knowledge Node package — deliberately not the "multi-object package" the UX-14.5.10 discovery ruled out of scope. **Relationship resolution strategy**: both endpoints resolved through the exact same `resolveCanonicalNode` primitive Knowledge Node import already uses, reused completely unmodified; only once *both* resolve does `upsertKnowledgeEdges` (also reused, extended additively) write the edge — this ordering *is* the "never create broken links" guarantee, verified by tests proving the edge is never written when either endpoint resolution fails. "Missing endpoint" is treated as a validation-time concept, not a runtime resolver failure (resolveCanonicalNode always succeeds by creating a node when needed): "orphan endpoints" is interpreted and rejected as a self-loop (both endpoints normalizing to the same title + nodeType), reusing the same identity rule `buildEdgeInputsFromRelationships` already applies during extraction. **Duplicate handling** falls out of composing the two reused primitives — re-importing the same package resolves to the same existing nodes and updates the same edge via `upsertKnowledgeEdges`'s existing conflict key, never creating a second one. `generatedBy` is preserved verbatim from the package (unlike `source_type`/`source_id`, which must be re-attributed to Knowledge Exchange) since it's portable, free-text provenance that stays accurate regardless of which account holds the row. **One small, additive extension to existing app code, not the framework**: `UpsertKnowledgeEdgeInput` gained an optional `metadata` field so import can record `importedFrom` provenance — `knowledge_links.metadata` is a real column, closing a gap Assets/Documents had to accept instead. Backward-compatible by construction (only included in the upsert payload when a caller provides it) — a correctness fix caught during implementation: an earlier draft would have unconditionally overwritten an existing edge's metadata to null on the next AI re-upsert; fixed before landing, and `knowledge-intelligence`'s full pre-existing test suite re-run to confirm zero behavior change for its two other callers. New UI: a per-relationship "Export" link added to each row in `KnowledgeNodeDetailPage`'s "Related concepts" list (not a single page-level button, since one node has several relationships), and a new "Import Link" button on `KnowledgeExplorerPage`'s actions row next to the existing Knowledge Node import button (renamed "Import" → "Import Node" for clarity) — opening `ImportKnowledgeLinkPackageDialog`, reporting each endpoint's created-vs-matched outcome independently. No framework redesign, no migrations, no RLS changes | ⚙️ | main | ❌ | — | ✅ (172 files/1373 tests — 50 new across `exportKnowledgeLinkPackage.test.ts`, `knowledgeLinkFetch.test.ts` (source/target direction resolution in both orientations), `validateKnowledgeLinkPackage.test.ts` (including the orphan-endpoint self-loop rejection and a same-title-different-nodeType case correctly accepted), `importKnowledgeLinkPackage.test.ts` (endpoint resolution, relationship creation ordering, provenance, two failure-injection tests), and a top-level `knowledgeLinkPackageRoundTrip.test.ts` (full cross-account round trip, duplicate-import handling, four rejected-before-any-resolver-call negative cases); one flake caught and fixed at the source during this phase (the same millisecond-boundary `Date.toISOString()` race pre-existing in other package types, this time fixed via `vi.useFakeTimers()` rather than propagated as a fourth instance); tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §28) |
| UX-14.5.10.5 — Knowledge Collection Exchange Discovery (discovery, no code): designs the final and highest-level Knowledge Exchange package type, grounded in the real `knowledge_collections` schema/API and every UI surface that touches collections. **Two corrections to the task's own assumptions, found by audit**: `knowledge_link` is not a valid collection member type — the real `CollectionItemType` union is `document`/`note`/`conversation`/`asset`/`knowledge_node`, confirmed against every real `AddToCollectionButton` call site — and a collection cannot contain another collection either (`knowledge_collection` never appears as a `target_type` anywhere), so circular references are structurally impossible today, not merely unlikely. **The single most consequential gap found**: no Conversation Exchange package type exists at all (no `src/modules/knowledge-exchange/conversations/`, only a title lookup) even though `conversation` is a real, live member type — resolved as an explicit, honest scope boundary (exported as an `unsupported: true` title-only stub, always reported as skipped on import) rather than invented under pressure as a side effect of this phase. **Package structure**: a `memberType`-discriminated manifest embedding each of the four real, already-built package types (Knowledge Node/Note/Asset/Document) verbatim as member payloads — no new per-type export/import logic, only orchestration. **Recommends the Collection package itself be a ZIP archive** (already-present `jszip`), not JSON, specifically because Document members are themselves ZIP archives and a JSON wrapper would force re-inflating through base64 a problem Document Exchange's own discovery already solved by choosing ZIP — recommended shape: `manifest.json` plus one nested `document-members/<index>.zip` entry per Document member. **Ordering is deliberately not treated as a reconstructable property** — confirmed no position/order column or reorder UI exists anywhere in the product; each member's original `created_at` travels only as inert display metadata, avoiding invented, speculative architecture. **Import algorithm — deliberately partial-success, not all-or-nothing** unlike Knowledge Link Exchange's own atomic rule: the collection row is created first and always succeeds, each member resolves independently through its own type's existing unmodified path, and one member's failure never discards already-successful ones — a principled difference (a relationship without both endpoints is meaningless, but a collection is a container of many independent things). A relationship *between* a collection's own knowledge-node members (not itself a membership type) is recommended as an auxiliary `relationships` array reusing `importKnowledgeLinkPackage` unmodified, replayed after every node member resolves. **Security audit**: confirms the collection wrapper introduces nothing new to check beyond what each embedded member's own already-audited exporter already verified. **Roadmap**: recommends four milestones — 10.5.1 container format + knowledge-node-only members, 10.5.2 note members + embedded relationships, 10.5.3 asset + document members (where the ZIP-of-ZIPs nesting and package-size-growth concerns actually get exercised) — with Conversation members left explicitly out of this roadmap entirely, pending a separate future effort. Deliverable: `docs/ux-14.5.10.5-knowledge-collection-exchange-discovery.md`. With this discovery complete, every object type in the original UX-14.5.10 Knowledge Exchange Expansion roadmap has either been implemented or has a complete, codebase-grounded blueprint. **Discovery only — no implementation performed** | 📝 discovery only | main | n/a | n/a | n/a (no code) |
| UX-14.5.10.6 — Knowledge Collection Exchange Implementation: implements the discovery (§29) exactly, in one pass rather than the discovery's own three-milestone split — the task named all four real member types plus the Conversation stub up front. New `src/modules/knowledge-exchange/knowledge-collections/`: `collectionPackageTypes.ts` (the `memberType`-discriminated `CollectionMemberEntry` union + manifest shape), `collectionPackageArchive.ts` (`buildCollectionPackageZip` — STORE-compressed outer container, one `document-members/<n>.zip` entry per Document member), `collectionMemberFetch.ts` (raw membership/node/node-to-node-relationship fetch helpers, mirroring `knowledgeLinkFetch.ts`'s own role), `exportKnowledgeCollectionPackage.ts` (pure manifest builder — zero per-type serialization, only orchestration) + `buildKnowledgeCollectionExportPackage.ts` (the async top-level fetch-and-assemble the Export button calls), `validateKnowledgeCollectionPackage.ts` (async two-pass structural/CRC ZIP validator, mirroring `validateDocumentPackage.ts` one level up, plus a 1GB total-package size cap), and `importKnowledgeCollectionPackage.ts` (the partial-success importer). **One small, additive change to an existing package type**: `validateDocumentPackage.ts`'s private manifest-shape checker was exported (renamed `validateDocumentPackageManifestShape`) so the Collection validator could reuse it verbatim for a Document member's JSON manifest half, rather than duplicating ~60 lines — its own behavior/tests/call site are otherwise untouched. **Validation strategy**: each `knowledge_node`/`note`/`asset` member's payload is validated by calling that type's own existing `validate<Type>Package` function directly (a Collection member embeds the *full* package object, so there's no separate member-payload shape to define); each `document` member's nested archive is independently re-verified via the unmodified `parseDocumentPackageZip`, one call per member — zero new zip-parsing logic. One deliberate, narrower choice than the discovery's own §8 musing: an unrecognized `memberType` string is a hard validation rejection of the whole package, not a soft per-member skip — building "tolerate a future, unrecognized member type" machinery for a member type nobody can currently produce would itself be speculative architecture; that's left for whenever a real sixth member type is actually designed. "Duplicate member identifiers" is checked as duplicate `archiveEntry` values across Document members, the one place a member entry carries anything resembling a machine identifier (titles are never treated as identifiers, since two Notes/Documents can legitimately share one). **Import algorithm — partial success, exactly per the discovery**: the collection row is created first and always succeeds; every member resolves independently through that type's own unmodified importer, each wrapped in its own try/catch, so one member's failure is recorded and the loop continues — nothing rolls back, the collection row is never deleted for a partial result. A `conversation` member is never attempted, always reported `skipped`. **Membership restoration**: `addItemToCollection` (unmodified) runs only on each member's own success, so only successfully-imported members ever become members of the new collection — a failed or skipped member is simply never linked. **Relationship replay**: auxiliary `relationships` replay only after every member has been attempted, each via the unmodified `importKnowledgeLinkPackage`, which independently re-resolves both endpoints by title via `resolveCanonicalNode` — correctly landing on whichever `knowledge_node` members this same import just created or matched, with no explicit id-passing needed; each relationship's own failure is caught and reported independently. New UI: an "Export collection" button on `KnowledgeCollectionDetailPage` (async `useMutation` calling `buildKnowledgeCollectionExportPackage` then `downloadBinaryFile`, the same pattern `DocumentDetailPage`'s own Export button already established) and an "Import Collection" button on `KnowledgeExplorerPage` next to the existing Import Node/Import Link buttons, opening `ImportKnowledgeCollectionPackageDialog` — the validation summary groups members by type (mirroring `KnowledgeCollectionDetailPage`'s own type-grouped display) and flags a conversation member as "not yet supported, will be skipped" before import even starts; the success screen reports every member's real outcome (imported/matched/skipped/failed, with each failure's own error message) grouped the same way, plus every relationship's own outcome — never a silently-absorbed failure. **Zero migrations, zero RLS changes, zero framework redesign** — `packages/{PackageVersion,PackageValidator,PackageExporter,PackageImporter}.ts` remain byte-for-byte unchanged. With this implementation complete, every real, currently-supported `CollectionItemType` except `conversation` is exchangeable, and the Knowledge Exchange roadmap first scoped in `docs/ux-14.5.10-knowledge-exchange-expansion-discovery.md` is fully implemented | ⚙️ | main | ❌ | — | ✅ (177 files/1410 tests — 37 new across `exportKnowledgeCollectionPackage.test.ts`, `collectionPackageArchive.test.ts`, `validateKnowledgeCollectionPackage.test.ts` (unsupported/future version, malformed JSON, non-zip input, CRC-corrupted archive, duplicate archive entries, unrecognized member type, missing/corrupt nested Document archive), `importKnowledgeCollectionPackage.test.ts` (per-type import, matched-vs-created, conversation always skipped, partial success, relationship replay and its own partial success), and a top-level `knowledgeCollectionPackageRoundTrip.test.ts` (full export→real-zip→transfer→validate→import chain proving membership restoration, duplicate-node reuse across a second import, partial-failure reporting, and rejection of a hand-tampered version before any member is imported); tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §30) |
| UX-14.5.11 — Knowledge Export & Download Experience: transforms the Knowledge Exchange package framework's existing technical export capability (Notes/Knowledge Nodes/Knowledge Collections) into a polished, user-facing "Save As…" experience. **Discovery finding**: no PDF or DOCX generation library exists anywhere in this codebase (`mammoth`/`pdfjs-dist` are read-only extractors — confirmed `pdfjs-dist` can't even run in this test environment, per `pdf.test.ts`'s own header comment — and `xlsx` writes only its own format), so two new, well-established, MIT-licensed dependencies were added: `docx` (9.7.1) and `jspdf` (4.2.1); `npm audit` confirmed zero new vulnerabilities from either. **Architecture: pure client generation (no edge function, no background job, no new table)** — every object's content is already loaded on its own detail page before Save As opens. New `src/modules/export/`: `types.ts` (`ExportContent`/`ExportSection` — an allow-list by construction, only plain strings can ever appear — `EXPORT_FORMAT_OPTIONS`, `exportFilename`, `assertNoSensitiveContent`), `exportService.ts` (`generateExport` — the one dispatch point; "UI only orchestrates"), `exporters/{markdownExporter,pdfExporter,docxExporter}.ts` (the latter two dynamically `import()` their heavy library, the exact lazy-loading discipline `processing/extractors/registry.ts` already established for `pdfjs-dist`/`mammoth`/`xlsx` — confirmed in the production build: both land in their own on-demand chunks, main bundle growing only ~5KB gzipped), `content/{note,knowledgeNode,collection}ExportContent.ts` (pure adapters turning each object type's already-loaded data into normalized `ExportContent` — `knowledgeNodeExportContent.ts` generalizes the pre-existing `buildKnowledgeExportMarkdown`'s own section structure), `hooks/useExportDownload.ts`, and `components/KnowledgeExportDialog.tsx` (the one reusable Save As / Download dialog, replacing the three previously isolated single-format "Export" buttons on `NoteDetailPage`/`KnowledgeNodeDetailPage`/`KnowledgeCollectionDetailPage` with a radio-button format picker — NOVA Package / PDF / Markdown / Word — and a single Download action; the separately-shipped, older Knowledge Actions v1 "Export knowledge package" markdown-only button on `KnowledgeNodeDetailPage` was deliberately left unchanged, not folded in, to keep this phase's blast radius to exactly the buttons the task named). **The NOVA Package format bypasses this entire new module** — it calls straight through to that object type's own existing, unmodified Knowledge Exchange exporter (`exportNotePackage`/`exportKnowledgeNodePackage`/`buildKnowledgeCollectionExportPackage`), only adding a `.nova` filename convention on top of already-audited bytes; the 3 corresponding Import dialogs' file-input `accept` attributes were additively widened to also accept `.nova` (zero change to actual parsing/validation logic) so the new extension round-trips cleanly. **Security**: `ExportContent`'s shape structurally excludes ids/embeddings/`ai_memory`/provider config (no field exists to carry them), plus a runtime `assertNoSensitiveContent` deny-list scan (`user_id`, `workspace_id`, `ai_memory`, `provider_override`, `embedding`, `access_token`, `refresh_token`) as defense-in-depth before any human-readable format is generated. No migrations, no RLS changes, no change to `packages/{PackageVersion,PackageValidator,PackageExporter,PackageImporter}.ts` or any existing Knowledge Exchange package type's own export/import/validate logic | ⚙️ | main | ❌ | — | ✅ (185 files/1457 tests — 47 new across `types.test.ts` (format list, filename slugify, one sensitive-content-guard test per deny-list term), `markdownExporter.test.ts`, `pdfExporter.test.ts` (structural validity via the `%PDF-` magic header, 200-item pagination without throwing), `docxExporter.test.ts` (unzips the generated `.docx` via the already-present `jszip` dependency and asserts the real `word/document.xml` contains the expected text — genuine content-correctness, not just a smoke test), `exportService.test.ts` (per-format dispatch, nova never touching content, the sensitive-content guard blocking markdown/pdf/docx but never nova), and one test file per content adapter (each asserting correct grouping/labeling and that no source id ever leaks into the output); tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §31) |
| UX-14.5.12 — Conversation Exchange: closes the last remaining Knowledge Exchange gap (§29.3/§30.8 of the discovery — Collections previously exported conversation members as an honest `unsupported: true` stub). **Discovery** confirmed the transferable allow-list (title, creation date, message role/content/timestamp) and the never-export list (`message_embeddings`, `ai_requests`, `provider_overrides`, `provider_id`, `context_chunk_ids`, any `'system'`-role content) against the real schema/RLS boundary in `0031_shared_knowledge_objects.sql`'s own header comment. New `src/modules/knowledge-exchange/conversations/`: `conversationPackageTypes.ts` (`ConversationPackage` — an allow-list by construction, no id/user_id/workspace_id/provider_id/context_chunk_ids field exists in the type at all), `exportConversationPackage.ts` (pure, filters non-user/assistant messages, preserves order), `validateConversationPackage.ts` (+`parseConversationPackage`, rejects empty-messages/invalid-role/malformed packages), and `importConversationPackage.ts` (always `createConversation` — insert-only, so overwrite is structurally impossible; messages inserted sequentially to preserve ordering; re-runs `indexMessage`+`linkKnownConceptsToSource` per message, the same fire-and-forget pipeline a live chat turn already runs, so an imported conversation is indistinguishable from one built by chatting). `PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter` untouched. **Collections integration**: `CollectionMemberEntry`'s conversation case is now `{memberType: 'conversation', originalAddedAt, payload: ConversationPackage}` — a collection export now genuinely includes notes, documents, assets, knowledge nodes, conversations, and relationships; import routes it through the unmodified `importConversationPackage` inside the same per-member try/catch every other member type already uses (partial-success policy unchanged). No circular references possible — `CollectionItemType` still has no `knowledge_collection` case. **Human-readable export + UI**: new `content/conversationExportContent.ts` adapter reuses the existing `src/modules/export/` module (no exporter duplicated) — one transcript section, `You:`/`NOVA:` role labels, matching `conversationToNote.ts`'s own convention. `ChatPage` gains a "Save As…" button (NOVA Package/PDF/Markdown/Word) next to the existing "Save to Notes"/"Add to Collection" actions; `KnowledgeExplorerPage` gains an "Import Conversation" button next to Import Node/Link/Collection, opening a new `ImportConversationPackageDialog` (mirrors `ImportNotePackageDialog`'s shell exactly) backed by `useImportConversationPackage`. **Security**: cross-account import always creates a brand-new conversation owned by the importer in the importer's chosen workspace — the exporter's identity never appears in the package, verified by a dedicated round-trip assertion; RLS on `conversations`/`messages` unchanged, `ai_memory`/`ai_requests`/`provider_overrides` never read by export and have no field to carry them in the package type. No migrations, no RLS changes, no framework redesign | ⚙️ | main | ❌ | — | ✅ (190 files/1493 tests — 33 new: 21 conversation-package unit tests + a 6-test top-level `conversationPackageRoundTrip.test.ts` + 5-test `conversationExportContent.test.ts`, plus Collection Exchange's own existing test files updated in place to assert `imported` with a membership link instead of the old `skipped` stub outcome; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §32) |
| UX-15.1 — Knowledge Experience Consolidation & Export Center: a UX consolidation phase, not a new capability — closes the discoverability/consistency gaps the UX-14.5.x Knowledge Exchange track left behind now that all six object types have their own export/import machinery. **Discovery** (full audit in `docs/ux-15.1-knowledge-experience-consolidation-discovery.md`) found: Document/Asset never got the unified Save As experience (still `.zip`/`.json`-only single-format Export, while Note/Conversation/Knowledge Node/Collection already had NOVA Package/PDF/Markdown/Word); Import entry points scattered across three unrelated pages (`NotesPage`/`LibraryPage`/`KnowledgeExplorerPage`) with inconsistent button wording (bare "Import" vs. type-labeled); `KnowledgeNodeDetailPage` had three different things labeled "Export"; no object's list-level card menu ever exposed Export/Save As/Add to Collection; "Ask AI" existed for only 2 of 6 object types under two different mechanisms/labels; "Share" has no real action anywhere (sharing is entirely passive); and all 8 Import/Export dialogs independently hand-rolled an identical native `<dialog>` shell. **Fixes**: new `content/documentExportContent.ts`/`content/assetExportContent.ts` adapters + `DocumentDetailPage`/`ImageReaderPage` now use the same `KnowledgeExportDialog` every other object type uses (the one functional gap closed for full parity); `NoteDetailPage`/`KnowledgeNodeDetailPage`/`KnowledgeCollectionDetailPage` each gained an "Ask NOVA about this…" button converging on the same `createConversationWithQuery` mechanism Assets already used (Document's own document-scoped `?documentId=` chat link left unchanged as a genuinely different integration); `KnowledgeNodeDetailPage`'s three "Export" labels disambiguated ("Export Briefing (Markdown)"/"Export relationship"/"Save As…"); `Share` left as passive `SharingBadge`/`OwnershipLine` per the discovery's own recommendation (no new backend capability invented). **Export Center**: new `exportCenterRequests.ts` (`buildExportRequestForObject` — one dispatch function per object type, fetching exactly what that object's own detail page already fetches and building the identical `ExportRequest`) and a new `ExportCenterPage` at `/knowledge/export` (linked from the sidebar) — six tabs, one per object type, each listing objects via that type's own existing list hook with multi-select checkboxes, a shared format picker, and one "Export N items" action reporting per-item pending/exporting/done/error status. **Consistency**: new shared `src/shared/components/ui/Dialog.tsx` — the one native `<dialog>` shell (`showModal`/`close`, synced via `useEffect`) all 7 Import dialogs + `KnowledgeExportDialog` previously hand-rolled independently (8 identical copies); every one now renders `<Dialog>` instead, a pure refactor with zero behavior change. No change to `PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter`, no package format changes, no migrations, no RLS changes, no new backend services | ⚙️ | main | ❌ | — | ✅ (193 files/1507 tests — 14 new: `documentExportContent.test.ts` (5), `assetExportContent.test.ts` (3), `exportCenterRequests.test.ts` (6); tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §33) |
| UX-15.2 — Personal Intelligence Workspace: a UX and orchestration phase — turns the Home/Hub landing experience into a Personal Intelligence Workspace, no architecture redesign, no AI service changes, no Knowledge Exchange changes, no schema/RLS changes. **Discovery** (full audit in `docs/ux-15.2-personal-intelligence-workspace-discovery.md`) found four separate pages competing to be "the" intelligence overview with real component overlap (Hub/Evolution both render `MaturityBadge`/`EvolutionTimelineSection`/`ConceptEvolutionSection`; Hub/Dashboard both render `RecommendedActionsSection`); "recent activity" independently fetched and rendered three separate ways (`RecentNotesSection`/`ActiveConversationsSection`/`CollaborationSection`'s own inline feed) with no canonical surface; Hub as a 12-section undifferentiated flat stack; 11 flat sidebar items, 4 of them overlapping overview pages; Dashboard/Hub disagreeing on voice (personalized greeting vs. static title); and `useCommandContext()`'s existing `inProgressDocument` (backed by `getMostRecentReadingProgress`) unused by any overview page despite being exactly what a "Continue Working" card needs. **Workspace design + cards**: Hub's 12 sections regrouped into named zones (Continue Working / Needs Your Attention / Recent Activity / Organize / Explore Deeper) answering the task's own "what am I working on/what deserves attention/what changed/what to organize/what to learn" questions — no new data fetching, only regrouping already-fetched `useWorkspaceHub()` data; Dashboard's/Evolution's own distinct content (scores, analytics, forecast, journey) isn't duplicated into Hub, "Explore Deeper" links out to both instead. New shared `src/modules/hub/components/RecentActivityList.tsx` (`leading`/`trailing` slots) is the one {title, link, relative-time} row renderer — `RecentNotesSection`/`ActiveConversationsSection`/`CollaborationSection` all now delegate to it, each keeping only its own data-shaping. New `ContinueWorkingCard` surfaces "continue where you left off" on the Hub itself (previously only reachable via the Command Bar), backed entirely by the existing `getMostRecentReadingProgress` data. **Context awareness**: `CommandContext.inProgressDocument`'s type widened from an ad hoc `{id, title}` to the full existing `InProgressDocument` (adds `updatedAt`, already returned by the API but previously untyped past `id`/`title`) — no new query, no new hook, no new context field. **Navigation**: `Dashboard`/`Evolution` removed from `Sidebar.tsx`'s flat nav items (routes/pages untouched, still reachable via Hub's "Explore Deeper" links); `Collaboration` stays (genuinely distinct member-management job) — reduces the sidebar's overlapping-overview surface from four entries to one. Hub's header now uses the same `useGreeting()` `ExecutiveDashboardPage` already used, closing the voice-inconsistency finding. No change to `PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter`, no Knowledge Exchange changes, no AI orchestration changes, no migrations, no RLS changes, no new Edge Functions | ⚙️ | main | ❌ | — | ✅ (193 files/1507 tests — 0 new test files; 4 pre-existing fixtures updated for the widened `InProgressDocument` type; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §34) |
| UX-15.3 — Intelligence Workspace & Proactive Assistant: shifts the Hub from "where everything is" toward "what matters now" — proactively-surfaced signals derived entirely from existing data, no AI generation, no new backend infrastructure. **Discovery** (full audit in `docs/ux-15.3-intelligence-workspace-discovery.md`) audited all ten named activity sources and found two hard gaps left explicitly out of scope rather than faked: command history has no persistence anywhere (no table, no `localStorage`), and export activity leaves no trace at all (export generation is 100% client-side); a third, partial gap — only Notes/Knowledge Nodes/Knowledge Links stamp `generation_metadata.importedFrom` on import, not Documents/Assets/Conversations — scopes "recently imported knowledge" to the three types that actually carry the signal. Also found `dashboardInteraction.ts`/`hubData.ts` each independently wrote out the identical `hasMemoryToReview` boolean expression, and that Command Palette/Export Center/Knowledge Explorer never called the already-consolidated `generateRecommendations` engine at all. **Intelligence cards**: new `src/modules/hub/workspaceIntelligence.ts` (`computeWorkspaceIntelligence`, 16 tests) — a pure function producing zone-tagged items from data the Hub already fetches: documents stuck outside `ready`, knowledge nodes with zero AI-generated edges (generalizing `graphSignals.ts`'s existing `disconnected_concept` check from "first one found" to a full count), a stale pinned/favorited conversation, a collection with several new members in the last two weeks (via `knowledge_links` rows with `source_type = 'knowledge_collection'` — membership is a link row, not a column), a frequently-referenced note (via `knowledge_links` where either side is `'note'`), a teammate's recent note/conversation, and a recently-imported note/knowledge node. **Workspace priorities**: `WorkspaceIntelligenceHubPage.tsx`'s zones renamed to the task's own vocabulary (Continue Working / Requires Attention / Recently Active / Suggested Organization / Shared With You / Recently Learned, replacing UX-15.2's naming); each zone renders the new intelligence items (via a small wrapper reusing UX-15.2's shared `RecentActivityList` — no new list component) alongside its existing static content; UX-15.2's "Explore Deeper" zone is kept as a seventh zone. `hubData.ts` now fetches `listDocuments`/`listKnowledgeCollections` (both pre-existing functions) and an unlimited `listNotes` so the new signals have full tag/collection/ownership/metadata data the evolution snapshot's own reduced shapes don't carry. **Recommendation consolidation**: new `hasMemoryNeedingReview()` helper in `signalEngine.ts` replaces the literal duplicate expression in both call sites; new `useWorkspaceRecommendations()` hook wraps `generateRecommendations`'s `'dashboard'` scope with just the two small fetches it needs, now consumed by a new `<SuggestedForYou/>` component (reusing the existing `RecommendedActionsSection` verbatim) on `ExportCenterPage`/`KnowledgeExplorerPage`, and folded directly into `NovaCommandBar`'s existing command pool (deduped by id) rather than a separate visual block; `hubData.ts` also now computes the real `informationOrganizationScore` instead of the hardcoded `100` its own comment had flagged since UX-13.7. **Workspace Health**: new `src/modules/hub/workspaceHealth.ts` (`computeWorkspaceHealth`, 12 tests) covering knowledge connectedness, untagged notes (new `listTaggedNoteIds` bulk query in `noteTags.ts`, mirroring `listKnowledgeNodeSourcesForNodes`'s own pattern), orphaned documents, collections without members, reading progress, recent imports, and AI indexing progress — "recent backup/export status" is rendered as an explicit "not tracked" indicator rather than invented from a proxy, per the discovery's own gap. New `WorkspaceHealthPanel` renders the strip near the top of Hub. No change to `PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter`, no Knowledge Exchange changes, no AI orchestration/prompt changes, no migrations, no RLS changes, no new Edge Functions, no speculative backend work | ⚙️ | main | ❌ | — | ✅ (195 files/1538 tests — 31 new: 16 in `workspaceIntelligence.test.ts`, 12 in `workspaceHealth.test.ts`, 3 in `signalEngine.test.ts`; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §35) |
| UX-15.4 — Action Center & Unified Actions: closes the distance between insight and execution — every recommendation should be actionable without navigating elsewhere, and the six object types' independently hand-rolled action menus should stop being six copies of the same idea. **Discovery** (full audit in `docs/ux-15.4-action-center-discovery.md`, an Explore agent cataloguing every action across Notes/Documents/Assets/Conversations/Knowledge Nodes/Knowledge Collections/Hub/Knowledge Explorer/Export Center/Collaboration/Search/Command Registry with exact file/line grounding) found: one shared `DropdownMenu` shell exists but zero shared "menu builder" (8 call sites each hand-roll their own item list); Save As (format-picker export) is the one action type with full 6-type coverage, already unified via `KnowledgeExportDialog`/`ExportCenterPage`; Rename absent for Note/Knowledge Node/Knowledge Collection; Delete absent for Knowledge Node; Add to Collection never on a list-card, only detail pages; Pin/Favorite/Archive/Duplicate exist only for Conversations (Archive is a repeated *pattern*, not a shared mechanism — Workspaces have their own independent copy); Share doesn't exist as an action anywhere (confirms UX-15.1's own finding); Ask NOVA is inconsistent for Document (a different mechanism/label than the other five types); Import placement is inconsistent (Knowledge Node/Link/Collection/Conversation import buttons all live on Knowledge Explorer, not their own list page). **Unified Action Registry**: new `src/modules/actions/actionRegistry.ts` — a pure, static `ActionDefinition[]` (id/label/icon/objectTypes/destructive), mirroring the existing Commands system's registry-plus-live-handler split (mutations are React Query hooks that must run inside a component) — `objectTypes` reflects what's real today, not an aspiration (9 tests). **Universal Action Menu**: new `src/shared/components/actions/ActionMenu.tsx` replaces the hand-rolled `<DropdownMenu><DropdownMenuItem>` block `NoteCard`/`ImageCard`/`ConversationList`/`DocumentCard` each wrote independently — pure consolidation, every item/label/icon/permission gate preserved byte-for-byte; a `ResolvedAction`'s `id` is optional for a card's own bespoke action outside the unified taxonomy (e.g. "Save as Notes"). **Multi-select actions**: new `src/shared/hooks/useMultiSelect.ts` extracted from `NotesPage`'s own pre-existing "Merge notes" selection state (Merge behavior unchanged); `NotesPage` gains bulk Delete/Add to Collection/Ask NOVA; `DocumentCard`/`DocumentGrid` gain the same optional selection props `NoteCard` already had; `LibraryPage`'s Documents tab gains a "Select" mode with bulk Delete/Add to Collection. New `useBulkAddToCollection` (`Promise.all` over the exact `addItemToCollection` function `AddToCollectionButton` already calls — no batch endpoint) + `BulkAddToCollectionControl` (shared collection picker). Bulk Export is deliberately not duplicated — `ExportCenterPage` (UX-15.1) already is the multi-select bulk export feature for all six types; Move/Tag/Share bulk actions left out of scope (no single-item precedent for Share; Move/Tag would be new scope, not consolidation). **Contextual actions on recommendations**: `IntelligenceItem` (UX-15.3) gains a required `actionLabel` field ("Resume Conversation," "Open Collection," "View Concept," etc.) rendered via `RecentActivityList`'s existing `trailing` slot — Export Center/Knowledge Explorer/Command Palette recommendations needed no change, `RecommendedActionsSection` already rendered `Recommendation[]` as labeled clickable buttons. No change to `PackageVersion`/`PackageValidator`/`PackageExporter`/`PackageImporter`, no Knowledge Exchange changes, no AI orchestration changes, no migrations, no RLS changes, no new Edge Functions | ⚙️ | main | ❌ | — | ✅ (197 files/1553 tests — 15 new: 9 in `actionRegistry.test.ts`, 6 in `useMultiSelect.test.ts`; tsc/lint/build all clean, implementation record in `docs/ux-14.5.4-collaboration-implementation-blueprint.md` §36) |
| UX-15.3.1 — Dark Mode Polish & Visual Consistency (Platform Polish Sprint, phase 1 of 6): first working dark theme the app has ever had — `color-scheme: light dark` was already declared but nothing backed it (`grep -rl "dark:" src` returned zero hits pre-phase). Full audit + fix in `docs/ux-15.3.1-dark-mode-polish-discovery.md`: ten new semantic status-color tokens (`--color-danger`/`-success`/`-warning`, each with `-strong`/`-bg` pairs) replace ~140 raw Tailwind literals (`text-red-600` etc.) project-wide; six new `--graph-color-*` tokens fix `GraphCanvas.tsx`'s two node colors that measured below the 3:1 non-text-contrast minimum against the dark surface; sixteen call sites pairing a theme-flipping background with hardcoded `text-white` fixed to `text-[var(--color-canvas)]` — the dominant pattern (`bg-[var(--color-ink)] text-white`, used for every active-tab/selected-pill affordance across `TagFilterBar`/`ExportCenterPage`/`KnowledgeExplorerPage`/`AiHealthPage`/`MemoryManagementPage`/`ImageReaderPage`/`WorkbookNav`/`ReaderPage`/`PdfPageControls`) would have rendered fully invisible white-on-near-white in dark mode; `--color-border` moved from stone-700 (1.70:1, failing) to stone-500 (3.65:1, passing) against the dark canvas. Non-obvious finding: overriding `--shadow-raised` etc. at `:root` in the dark media query is silently inert — Tailwind v4 bakes `@theme` shadow values into each `shadow-*` utility's own `--tw-shadow` at build time (confirmed zero `var(--shadow-*)` references in compiled output), so the actual fix overrides `--tw-shadow` on the utility class names themselves, same specificity, later in cascade order. All dark-mode text/UI pairings checked against WCAG contrast minimums (4.5:1 text / 3:1 non-text) — full table in the discovery doc, all pass. No manual toggle added (single `prefers-color-scheme` media query, matching the pre-existing `color-scheme: light dark` declaration's own implied mechanism); no Knowledge Exchange/AI orchestration/migration/RLS/Edge Function changes | ⚙️ | main | ❌ | — | ✅ (197 files/1553 tests, unchanged — pure color-token substitution; tsc/lint/build all clean, discovery doc doubles as the implementation record) |

### Findings from UX-14.3 not fixed this milestone

- ~~**`detectMemoryCandidates` has no live caller**~~ **Fixed in UX-14.3.5** — now wired into `useSendMessage`, called on every turn on both `ChatPage` and `ReaderChatPanel`. `MemoryApprovalPanel`'s candidate queue is no longer always empty in production.

---

## Beta Invites, Platform Admin & AI Governance Foundation

Explicit priority change: the UX roadmap (UX-15.3.x/UX-15.4+) is paused, not abandoned — the existing UX-15.x work above is left exactly as it landed, nothing rolled back. This track closes a different gap: the beta/plan/quota system had been partially built by hand directly in Supabase, and no platform-operator (founder) surface or AI-provider entitlement model existed at all.

| Milestone | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Beta Invite + Quota System Reconciliation & Repair: full read-only audit of the live Supabase schema first (explicitly not trusting migrations/local code as source of truth, per the task's own instruction), documented in `docs/beta-invite-quota-reconciliation.md`. Found and repaired four real defects via one additive migration (`0034_beta_invite_quota_repair.sql`): a malformed `beta_invites.status` default, no case-insensitive email uniqueness (a `dan@x.com`/`Dan@x.com` duplicate-signup gap), a signup gate that only existed client-side (`enforce_beta_invite_gate` `BEFORE INSERT` trigger on `auth.users` added as the server-side backstop), and RLS fully disabled on four tables (`beta_invites`/`plans`/`plan_quotas`/`user_plan_assignments`/`quota_usage`, own-row-only `SELECT` policies added, zero client write policies). Root-caused and fixed the two actual causes of pre-existing failures found mid-audit: a stale hand-maintained `database.ts` missing the new tables' types, and a missing test mock in `AIService.test.ts`. `consume_quota()` RPC added as the one sanctioned path to increment usage (atomic, period-scoped) | ⚙️ | main | ❌ | — | ✅ (9 new in `quotaService.test.ts`, 3 new in `AuthContext.test.ts`) |
| Pause UX Roadmap & Complete Beta / Admin / AI Governance Foundation: full read-only discovery first (`docs/beta-admin-ai-governance-discovery.md`, 12 sections, live-database-grounded), then a new `platform_admins` table + `is_platform_admin()` (additive migration `0035_platform_admin_foundation.sql`) — deliberately a separate table with zero client write policies, not a `profiles.is_admin` column, since `profiles` already has a self-service RLS `UPDATE` policy that would make a boolean column self-assignable (a direct violation of "founder privileges cannot be self-assigned"). Four `SECURITY DEFINER` admin RPCs (`admin_list_users`/`admin_list_beta_invites`/`admin_create_beta_invite`/`admin_revoke_beta_invite`), each internally re-checking `is_platform_admin()` and raising on a non-admin caller — authorization is server-enforced, not UI-hidden, mirroring the existing `has_workspace_role()`/`list_workspace_members()` pattern. New `AdminDashboardPage` (Overview/Beta Invites/Users/Plans & Quotas/AI Providers/System Health, all real data — no fabricated metrics) behind a new `RequireAdmin` route guard, replacing the old unauthenticated `/admin/beta` stub. New `src/modules/plans/` (`getCurrentUserPlan`, `canSelectProvider(planCode)` — `pro`/`enterprise` only). Provider identity now fully invisible to Free/Beta everywhere: `ProviderSelect` removed unconditionally from `ChatPage`/`ReaderChatPanel` for every plan (not gated — never shown in the normal chat composer, per spec); Pro/Enterprise/Founder get a single provider picker on a new gated `/settings/advanced` page only; the rich Provider Control Center (test/enable-disable/health) and `AiHealthPage`/`ProviderHealthDetailPage` moved from "every signed-in user" to admin-only, inside the Admin Dashboard. Security audit confirmed (background agent, file/line-grounded) no provider API key ever leaves the server — the three real keys are read exclusively inside `supabase/functions/ai-chat` and `provider-availability` via `Deno.env.get`; the only client-side secret-shaped value is the public Supabase anon key. No Knowledge Exchange/export-import/AI-orchestration changes, no platform-wide provider override capability built (would be new schema beyond what this phase needed) | ⚙️ | main | ❌ | — | ✅ (15 new: 6 in `entitlements.test.ts`, 6 in `adminApi.test.ts`, 3 in `RequireAdmin.test.ts`; full suite 202 files/1580 tests, tsc/oxlint/build all clean) |
| Founder Command Center & AI Governance Phase: transforms the single-page admin foundation into a professional SaaS operations console — full read-only discovery first (`docs/founder-command-center-discovery.md`), migration `0036_founder_command_center.sql`. **Real defects found and fixed before building on top of them**: `user_plan_assignments`'s literal `UNIQUE(user_id, active)` constraint capped every user at exactly one plan change ever (deactivating a second time would violate it) — replaced with a partial unique index (`WHERE active`), mirroring `0028_workspace_members.sql`'s identical pattern; the prior dashboard's "AI requests"/"Recent AI errors" stat tiles silently read `ai_requests` under its own-row-only RLS, showing only the founder's personal chat activity, not the platform's; the "AI Providers" enable/disable toggle wrote to the founder's *personal* `provider_overrides` row, not any platform-wide state — genuinely misleading for something labeled AI governance; a project-level `ALTER DEFAULT PRIVILEGES` grants `EXECUTE` on every new function directly to `anon` (not just `PUBLIC`), so `revoke ... from public` alone (the pattern every admin function to date used) never actually restricted anonymous execution — never an authorization bypass (each function's own `is_platform_admin()` check still rejected an unauthenticated caller) but real hardening was missing; fixed with explicit `revoke ... from anon` on every function this phase touches, including a scoped tightening pass on the prior phase's five. **New capabilities**: `admin_change_user_plan`/`admin_reset_user_quota`/`admin_set_user_disabled`/`admin_update_plan_quota`/`admin_set_platform_provider_setting`/`admin_ai_usage_summary`/`admin_platform_counts` — all `SECURITY DEFINER`, all admin-checked, additive to the existing pattern. Account disabling uses real `auth.users.banned_until` (checked by Supabase Auth on sign-in/refresh, not a cosmetic flag), with self-disable explicitly blocked since granting admin has no client-reachable path. New `platform_provider_settings` table (authenticated-read-all, write only via RPC) is the first genuine platform-wide AI governance surface — `resolveProviderChain()` gained one additive, optional `platformSettings` parameter (existing tests unchanged, behavior identical when omitted) that lets a disabled-platform-wide provider be excluded from *every* user's routing, not just the founder's own, and lets priority order the non-preferred remainder ahead of health score. **New surface**: `/admin/users` (view/change plan/reset quota/disable, all via RPC, no direct table writes), `/admin/plans` (quota-limit editing), `/admin/ai` (platform-wide provider enable/disable/priority, usage last 7 days) — `/admin` (Overview) trimmed to real platform-wide stat tiles (Users/AI/Knowledge/System) plus Beta Invites, linking out to the three new pages rather than duplicating their tables. No UX-15.x/Knowledge Exchange/AI-orchestration-redesign work | ⚙️ | main | ❌ | — | ✅ (full suite 202 files/1594 tests — 17 in `adminApi.test.ts` incl. 10 new, 3 new in `resolveProviderChain.test.ts`; tsc/oxlint/build all clean) |

| AI Preference Layer v1 — User Preferences + Auto Recommended Routing: full read-only discovery first (`docs/ai-preference-layer-discovery.md`) resolved the directive's own cut-off question — no new table needed, since `profiles.default_chat_provider_id` (nullable, Phase 8B) already is a correct one-column-per-user preference model where `null` already means "no preference." The real bug was entirely in resolution, not storage: `resolveDefaultChatProviderId()` silently substituted the hardcoded `DEFAULT_CHAT_PROVIDER_ID` constant whenever a user had no eligible preference, so "Auto" never actually reached `resolveProviderChain()`'s own correct automatic (platform governance → availability → preference → health-ordered fallback) ordering — every one of the 8 call sites downstream (chat, title generation, briefings, notes, flashcards, chapter summaries, command actions) always saw *some* concrete "preferred" id and never took the true automatic path. Fixed at the source: `resolveDefaultChatProviderId`/`useDefaultChatProviderId` now return the user's raw preference or `null` (no substitution); `resolveProviderChain`/`useProviderChain` widened to accept `string | null` (zero behavior change for a real preference — `eligibleIds.includes(null)` was already false, already producing the correct automatic ordering). `ProviderSelect` gained its first-ever "Auto (Recommended)" option (`value=""` ↔ `null`), and `AdvancedSettingsPage` now passes the raw preference straight through instead of pre-resolving it — this is the actual fix for "Advanced Settings does not show Auto (Recommended)." `useSendMessage` now exposes its resolved `chain`, so `ChatPage`/`ReaderChatPanel`/`useCommandActions` stamp a brand-new conversation's one-time `provider_id` with what NOVA would genuinely pick right now (chain[0]) instead of the hardcoded constant, when there's no explicit preference. Explicitly scoped out and documented as future work: `conversations.provider_id` is non-nullable, so an existing conversation's stamped provider still wins on every subsequent send in that conversation — true per-message re-evaluation for already-created conversations would need a schema change (nullable `provider_id`) and touches the already-shipped, tested conversation-provider-switching feature; not built this "v1" pass. No new migrations, no schema changes, no Knowledge Exchange/Founder Command Center changes | ⚙️ | main | ❌ | — | ✅ (8 new: 4 in `defaultProvider.test.ts`, 4 in `ProviderSelect.test.ts` — its first-ever test file, no prior render-test precedent for this component; full suite 204 files/1602 tests, tsc/oxlint/build all clean) |

Manual Supabase verification checklist (what to look for, why it matters, whether to change anything — no blind SQL): `docs/beta-admin-ai-governance-supabase-checklist.md` (Beta/Admin/AI Governance Foundation) and `docs/founder-command-center-supabase-checklist.md` (Founder Command Center & AI Governance Phase). AI Preference Layer v1 made no schema changes, so no checklist entry was needed for it.

## AI Experience Intelligence v1

Full read-only discovery first (`docs/ai-experience-intelligence-discovery.md`): the codebase already implements most of the brief's six pillars — a signal/attention/recommendation type system (`src/modules/intelligence/signals`, `orchestrator`, `recommendations/recommendationEngine.ts`), all composed by `orchestrator.ts` into `InteractionState` and consumed by both Chat and the Hub. Every one of those types is explicitly self-documented as "informational, never auto-acted-on." Scope closed the concrete gaps found, extending existing systems rather than building a parallel one, per the task's own "do not create duplicate intelligence engines" rule.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Confidence-weighted memory retrieval: `formatMemoriesForPrompt`/`isMemoryUsedByPrompt` now rank by `ai_memory.confidence` (desc, nulls last) before recency, via one new shared `rankMemories.ts` — closes the one real gap in the memory system found by discovery (confidence computed since `0027_ai_memory_confidence.sql`, never read back at retrieval time) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (4 new in `rankMemories.test.ts`; zero regressions across all 8 pre-existing memory test files) |
| Reader conversation title parity: `ReaderChatPanel` now calls the same `useGenerateConversationTitle` `ChatPage` already uses, gated on `isNewConversation` (equivalent to ChatPage's `title === 'New conversation'` check, expressed without depending on this panel's own conversations cache having caught up) — Reader-originated conversations no longer stay "New conversation" forever | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (existing `useGenerateConversationTitle`/title-generation coverage unchanged; this is a call-site wiring change) |
| Persistent suggestion dismissal: new additive migration `0037_dismissed_suggestions.sql` (`user_id`, `workspace_id`, `item_key`, own-row RLS, matching `workspace_objectives`' pattern exactly) + `src/modules/intelligence/dismissals/{api,useDismissedSuggestions}.ts` — the one piece of genuinely new infrastructure, closing the single gap both discovery audits converged on: no AI-generated suggestion (signal/attention item/recommendation) had any persistent "I've seen this" mechanism anywhere in the app | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (thin Supabase-backed API/hook, exercised via the two consumers below; no new business logic to unit-test beyond `recommendationItemKey`) |
| Dismissal wired into the Hub: `RecentActivityList` gained an optional `onDismiss` (opt-in, every other consumer — `RecentNotesSection`/`ActiveConversationsSection`/`CollaborationSection` — unaffected); `IntelligenceZoneItems` (all 4 zones: attention/organize/shared/learned) and `RecommendedActionsSection` (new optional `dismissedKeys`/`onDismiss` props, hover-only "✕") now filter out and let the user dismiss individual items, keyed by `IntelligenceItem.id` or the new `recommendationItemKey` (`category:command.id`) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (2 new in `recommendationItemKey.test.ts`) |
| Unresolved-conversation continuation nudge: new `listLastMessageRoles` (one query, not N+1) detects a conversation whose last message role is `'user'` — a turn that never got an assistant reply — among the Hub's already-fetched `activeConversations`; surfaced as a real, evidence-backed `continue`-category recommendation via `recommendationEngine.ts`'s existing dashboard branch (reuses `buildResumeConversationCommand`, no new command) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (2 new in `recommendationEngine.test.ts`) |
| Explicitly deferred, with reasoning (see discovery doc §5): general LLM-based intent classification (UX-12's `intentClassifier`/`contextSelector` already cover this deterministically; extending that pipeline is the correct future path, not a second classifier); semantic conversation-to-conversation linking (`resurfacingEngine` stays recency-based — true topical linking needs embedding-based similarity search, a materially larger build); automation/scheduled proactive execution (confirmed to not exist anywhere in the frontend; this phase's proactive item is user-visible-and-dismissible, not autonomously executed); acting on feedback signals (no feedback-capture UI was in this phase's ground-truthed v1 scope — no `messages.feedback` column exists yet) | 🔲 | — | — | — | — |

## Multimodal Intelligence v1

Full read-only discovery first (`docs/multimodal-intelligence-discovery.md`). The central finding: no AI provider integration in this codebase could accept image input at all — every `ChatProviderMessage`/edge-function message was `content: string` by construction (confirmed by explicit "no vision/OCR this phase" disclaimers already in `ImageCard.tsx`/`ImageReaderPage.tsx`). Most of this phase is therefore the one real architecture extension needed (image content support), plus reuse of the existing document/knowledge-extraction pipeline for everything downstream — not a parallel intelligence engine.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Multimodal provider foundation: `ChatProviderMessage.content` widened to `string \| ChatContentPart[]` (additive — every existing string call site is unchanged); `supabase/functions/ai-chat/index.ts` gained per-provider content-block builders (`toAnthropicContent`/`toOpenAiContent`/`toGoogleParts`) translating an image URL into each provider's real wire format. Only the OpenAI path is actually testable in this deployment — `ANTHROPIC_API_KEY`/`GOOGLE_API_KEY` are not configured secrets here (same pre-existing limitation the text-chat path already has) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (edge-function Deno code, outside the vitest surface, same as every other file under `supabase/functions/`) |
| `assets.metadata jsonb` (new additive migration `0038_asset_metadata.sql`) + `analyze-image` one-shot vision call (`src/modules/assets/intelligence/analyzeImage.ts`, via `streamChatCompletion` directly — capabilities are string-template-only and can't carry an image): produces a plain-language description and any visible text, framed honestly as AI interpretation, never as a calibrated OCR confidence score (no provider in this codebase exposes one) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (parser tested below; the network call itself is exercised by the "Analyze with NOVA" UI flow) |
| Visual knowledge extraction, by reuse: `runKnowledgeExtraction`'s concept/entity/relationship chain generalized into `runKnowledgeExtractionFromContent` (polymorphic `sourceType`/`sourceId`, same convention `knowledge_nodes`/`knowledge_links` already use); `runKnowledgeExtraction` itself is now a thin document-chunk-fetching wrapper around it, behavior-unchanged for every existing call site. `useAnalyzeImage` calls both steps as one "Analyze with NOVA" action, feeding the image analysis text through with `sourceType: 'asset'` | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (no new business logic beyond the parser; the refactor is behavior-preserving, verified by the full existing knowledge-intelligence test suite staying green) |
| "Analyze with NOVA" UI on `ImageReaderPage`'s Chat tab: replaces the previous "no vision/OCR this phase" disclaimer with a real analyze action, a result panel (description + visible text), and a "Re-analyze" control; copy is still careful not to claim NOVA can see the image mid-conversation — only that it can be asked to look once. `ImageCard`'s own comment updated to match | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — |
| Document Intelligence v1 (orthogonal to the above — pure text, no provider change needed): new `analyze-document-intelligence` capability classifying a document and extracting explicitly-stated dates/decisions/tasks, reusing the exact `boundContent`/`runCapability` pattern knowledge extraction established (`boundContent` now exported for this reuse); result persisted to `extraction_metadata.metadata.documentIntelligence` via a targeted update (not `saveExtractionMetadata`'s full upsert, which would have clobbered `chapters`/`spreadsheet`); `DocumentIntelligencePanel` added to Document Detail, mirroring `KnowledgeExtractionPanel`'s shape | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (7 new in `parseDocumentIntelligenceResponse.test.ts`, covering well-formed/fenced/missing-field/malformed-entry/no-JSON cases) |
| Explicitly deferred, with reasoning (see discovery doc §6): dedicated OCR with per-character confidence/layout (a materially different technology stack than "call a vision-capable chat model" — this phase doesn't substitute a lesser capability and call it the same name); handwritten notes → structured project/task/deadline conversion (needs first-class task/project entities with owners and deadlines, which don't exist yet); spreadsheet screenshots (chains two speculative capabilities instead of reusing the real `xlsx`/`csv` extractor); diagram/whiteboard *structural* (bounding-box-grounded) relationship extraction (the general analyze-image capability describes a diagram in prose, not a vision-grounded node/edge graph); a unified cross-surface "Upload anything" UX (this phase adds one entry point — Images tab / `ImageReaderPage` — not Chat attachments or Reader integration); making Anthropic/Google functionally vision-capable in this deployment (an operational secrets-configuration change, not a code gap); agentic features (explicitly excluded by this phase's own final instruction) | 🔲 | — | — | — | — |

## Multimodal Intelligence v2 — Visual Knowledge Intelligence

Full read-only discovery first (`docs/multimodal-intelligence-v2-discovery.md`). Confirms v1's own architecture reused throughout — no per-feature-area duplicate intelligence engine, no second vision call, no second knowledge-extraction chain. The load-bearing finding: no provider exposes a calibrated confidence score for vision output, so "confidence" here is the model's own self-reported estimate, always labeled as such — never presented as a statistical guarantee.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| `analyzeImage` extended (prompt + `parseImageAnalysisResponse.ts` + `AssetAnalysis` type): detected language and self-reported confidence (`{text, entities, relationships}`, each independently nullable) alongside the existing description/extracted-text, all in one vision call — not a second capability per "OCR/Screenshot/Handwriting" feature area, since the underlying mechanism is identical regardless of what the image actually contains | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (6 new cases in `parseImageAnalysisResponse.test.ts`: multi-marker parsing, marker-order independence, out-of-range/malformed confidence values, LANGUAGE: none handling) |
| Document Intelligence generalized: `runDocumentIntelligence` split into a thin document-chunk wrapper and `runDocumentIntelligenceFromContent` (mirroring v1's `runKnowledgeExtractionFromContent` generalization exactly) — reused verbatim by `useAnalyzeImage` to extract dates/decisions/tasks from an asset's analyzed text, stored in `assets.metadata.documentIntelligence`. Zero new capability, zero new prompt, zero new parser | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (behavior-preserving refactor for the document path, verified by the full existing documentIntelligence test suite staying green) |
| Asset search: new `asset_embeddings`/`match_assets` (migration `0039_asset_search.sql`, mirroring `note_embeddings`/`match_notes` exactly), `indexAsset.ts` (mirrors `indexNote.ts`, fires after "Analyze with NOVA" completes), `assetSearchProvider.ts` (mirrors `notesSearchProvider.ts`'s hybrid semantic+lexical shape), registered as Universal Search's fourth source. Closes Feature Area 1's explicit mandate — analyzed image content is now searchable, not dead text | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (thin Supabase-backed indexer/provider, same untested-at-this-layer precedent as `indexNote`/`notesSearchProvider`) |
| UI: `ImageReaderPage` now shows detected language next to visible text, a "needs review" banner when any self-reported confidence dimension is below threshold (`needsConfidenceReview`), and a summary of dates/decisions/tasks found. "Convert to structured note" renders that structured data as a real Note via `buildStructuredNoteContent` — a markdown checklist for tasks, sections for dates/decisions — the honest reading of "Notes + Knowledge Nodes + Tasks" without inventing a first-class Task entity (knowledge nodes already flow through v1's reused extraction chain) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (5 new in `assetConfidence.test.ts` + `buildStructuredNoteContent.test.ts`) |
| Explicitly deferred, with reasoning (see discovery doc §9): a duplicate intelligence engine per feature area (OCR/Screenshot/Spreadsheet-Vision/Handwriting all route through the one extended `analyze-image` capability); real calibrated OCR confidence (no such technology is integrated, and building one is a different project than this phase); a first-class Task entity with due-date reminders/notifications (tasks live inside a structured Note instead); rigorous spreadsheet-screenshot cell/formula reconstruction (would misrepresent vision-derived guesses with the same confidence as the real `xlsx` analyzer); vision-grounded (bounding-box) diagram structural extraction; functional Anthropic/Google vision (still an operational secrets gap, not code) | 🔲 | — | — | — | — |

## Knowledge Intelligence Layer v1

Full read-only discovery first (`docs/knowledge-intelligence-layer-v1-discovery.md`, two parallel audits — graph/relationship/confidence, and search/memory/recommendations/timeline). The central finding: relationship discovery, node confidence, relationship strength, structural gap detection, BFS/pathfinding, and graph rendering all already existed and were well-tested — the real gaps were narrower (two hard-partitioned graphs missing asset/conversation nodes, manual-only relationship discovery, no packaged relationship-confidence object, an ungrouped timeline, no query surface, no topical gap detection, concept search outside unified ranking). Every item below extends one of those existing systems.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Feature 1 — Knowledge Graph Visualization: `GraphNodeType` gained `asset`/`conversation`; `buildSourceGraphAdditions`/`mergeGraphLayers` (pure) turn a concept's resolved evidence into extra graph nodes/edges; `InteractiveConceptGraph` gained a "Show sources" toggle merging this second layer into the existing `GraphCanvas` render, BFS, and interactive controls — unchanged. The separate legacy Content Connections graph was left untouched | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (6 new in `graphSourceNodes.test.ts`) |
| Feature 2 — Automatic Relationship Discovery: `autoReconcileNewKnowledge` reuses the manual Reconcile button's exact capability/persistence path, scoped to a small bounded comparison set, fired fire-and-forget at the end of every extraction; tagged `ai:auto-detect-connections` (distinct from the manual button's tag) so `listRecentAutoDiscoveredConnections` can surface "New connections NOVA found" as a dismissible Knowledge Explorer panel, reusing the existing `dismissed_suggestions` infrastructure | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | — (composition over already-tested `buildEdgeInputsFromRelationships`/`upsertKnowledgeEdges`; no new pure logic beyond that reuse) |
| Feature 3 — Knowledge Confidence: `computeRelationshipConfidence` returns `{relationship, evidenceCount, sources}` — the edge's own persisted confidence plus a broader (all-source-type) shared-evidence intersection than the existing document-only `scoreRelationship`. Rendered under each related-concept row as "Backed by N shared sources (type, type)", never a bare percentage | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (6 new in `relationshipStrength.test.ts`) |
| Feature 4 — Knowledge Timeline: `groupEvidenceByPeriod` buckets the evidence `getKnowledgeNodeEvidence` already fetches into calendar-month periods, earliest-first — no new table or query, `knowledge_node_sources.created_at` was already the mutation log needed. Replaces the prior flat "Timeline" list on Knowledge Node Detail | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (7 new in `knowledgeTimeline.test.ts`) |
| Feature 5 — Intelligence Queries: `classifyIntelligenceQuery` is a deterministic rule table (same convention as `intentClassifier.ts`, no AI call) mapping free text to `evolution \| related_sources \| gaps \| patterns \| decisions`; `runIntelligenceQuery` dispatches to existing composers (timeline, evidence, graph insights, topical gaps) plus one new decisions aggregator reading already-stored `documentIntelligence.decisions`. Surfaced as `IntelligenceQueryBox` on Knowledge Node Detail, scoped to the concept already on screen | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (8 new in `intelligenceQueryClassifier.test.ts`) |
| Feature 6 — Knowledge Gaps: `detectTopicalKnowledgeGaps` adds one new adaptive capability (`detect-topical-knowledge-gaps`) answering the milestone's own topical example ("strategy + marketing but no financial model"), which the existing deterministic `computeKnowledgeGaps` can't reach — always rendered as a suggestion to confirm, never a detected fact, via a manually-triggered Hub section (same cost discipline as the Reconcile button) | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (6 new in `parseTopicalGapsResponse.test.ts`) |
| Feature 7 — Search Evolution: `conceptSearchProvider` registers the existing title-ILIKE concept lookup as a real `SearchProvider`, merging concept results into `runUniversalSearch`'s one ranked list instead of a second parallel call (SearchPage deduplicates against the richer `ConceptCard` section rather than showing each concept twice); `applyImportanceBonus`/`computeImportanceBonus` add a small, capped, uniformly-applied "importance" signal from `knowledge_node_sources` citation counts, alongside the existing recency bonus | ⚙️ | claude/pip-edge-function-deploy-9lzs8n | ❌ | — | ✅ (8 new in `importanceScore.test.ts`) |
| Explicitly deferred, with reasoning (see discovery doc): a second graph model/BFS/confidence formula/gap detector (everything above extends the existing ones); unifying the legacy Content Connections graph with the AI Knowledge Graph (a materially larger migration, not needed for Feature 1's own example); a `knowledge_node_embeddings` table or true semantic concept search (Feature 7 changed ranking/merging, not matching); a persisted graph-mutation event log (Feature 4 is answered entirely from existing `knowledge_node_sources.created_at`); a general-purpose NL entity resolver for Feature 5 (no existing capability to build on, confirmed by the discovery audit) | 🔲 | — | — | — | — |

## PIP Stabilization & Intelligence Integration v1

Full read-only discovery first (`docs/pip-stabilization-intelligence-integration-discovery.md`), then a formal Phase 2 Reconciliation and implementation record (`docs/pip-stabilization-intelligence-integration-v1.md`). Not a new intelligence capability — this milestone fixes three reachability/reliability gaps in already-built systems: chat's RAG retrieval was structurally blind to analyzed images, beta invite creation never sent an email, and the codebase had zero mobile-viewport-aware CSS anywhere. All three fixes reuse existing infrastructure exclusively.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| P0 fix — Image → Chat Intelligence Integration: chat's retrieval layer (`retrieveContext`) was hardcoded to `match_document_chunks` and `retrieveGraphContext` filtered `source_type = 'document'`, so an image's analyzed content — however well Multimodal Intelligence v1/v2 had analyzed it — could never reach a chat prompt. New `retrieveAssetContext.ts`/`buildAssetContextContent.ts` query the existing `match_assets` RPC and format results honestly (never fabricating missing sections); `retrieveGraphContext` extended to also accept `assetIds`; a new `<visual_context>` prompt block (mirroring the existing tagged-block convention) carries this into `buildSystemPrompt`; `ImageReaderPage`'s "Ask NOVA about this image" now seeds the conversation with real analyzed content via `buildImageChatSeedQuery.ts` instead of a bare title; image upload now auto-triggers analysis (fire-and-forget) instead of requiring a manual "Analyze with NOVA" click. Asset matches flow through a separate `AssetContextMatch[]` type, never overloading `VectorMatch`, so the existing document-chunk RAG path (including `resolveReferences`/`context_chunk_ids` persistence) is untouched | ⚙️ | main | ❌ | — | ✅ (`buildAssetContextContent.test.ts` 9, `buildSystemPrompt.test.ts` +3, `AIService.test.ts` +4, `buildImageChatSeedQuery.test.ts` 4) |
| P1 fix — Beta Invite Email Delivery: `admin_create_beta_invite` had only ever inserted a `beta_invites` row — no email was ever sent, on any deployment, since the feature was built. New `send-beta-invitation` edge function mirrors `send-workspace-invitation`'s exact trust boundary (caller JWT → `is_platform_admin()` RPC re-check → service-role read-only escalation → Resend HTTP API), reusing the same already-configured `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`SITE_URL` secrets. `AdminDashboardPage`'s invite form now reports invite-creation and email-delivery as two distinct, honestly-labeled outcomes | ⚙️ | main | ❌ | — | ✅ (`adminApi.test.ts` +3) — edge-function Deno code itself is outside the vitest surface, same as every other file under `supabase/functions/` |
| P1 fix — iPhone / Mobile UX: the codebase was uniformly `100vh`/`h-screen` with zero `dvh`/`env(safe-area-inset-*)` usage anywhere. Root cause of the reported "composer cut off" symptom: `ChatPage.tsx`'s `h-[calc(100vh-3.5rem)]` assumed a fixed `TopBar` height that was never true — replaced with `h-full`, self-correcting via the existing `AppShell` flexbox chain. `h-dvh`/`min-h-dvh` added as a progressive-enhancement fallback alongside every `h-screen`/`min-h-screen` in the app; `ImageReaderPage` now defaults to fit-within-container image sizing instead of forcing horizontal scroll on every photo; header button clusters gained `overflow-x-auto` safety; `env(safe-area-inset-bottom)` added to the chat composer and the minimized insight-drawer pill (requires `index.html`'s new `viewport-fit=cover` to resolve to non-zero values); `Dialog` gained an explicit `max-h-[85dvh] overflow-y-auto`. Every change is additive — the original CSS remains the fallback on browsers without the newer features, and none of it special-cases a device width or user agent | ⚙️ | main | ❌ | — | — (CSS/layout-only change; no test suite exists for this in the repo, consistent with every prior CSS-only change here — verified via `tsc -b`/`vitest run`/`oxlint`/`vite build` all passing, plus manual QA required on a physical device, see the v1 doc's Phase 7 checklist) |
| Explicitly deferred, with reasoning (see the v1 doc's "What remains" section): the asset-match reference-chip UI affordance (`resolveReferences` doesn't yet know about assets — content still reaches the model via `<visual_context>`, only the clickable-chip UI doesn't extend to assets yet); confirming `RESEND_API_KEY` is actually configured in this deployment (an operational check, not a code gap); all 19 manual verification items in the v1 doc, none of which an automated test in this codebase can confirm | 🔲 | — | — | — | — |

## PIP Multimodal Intelligence Stabilization v1

Full read-only discovery first (`docs/pip-multimodal-intelligence-stabilization-v1-discovery.md`), then an implementation record (`docs/pip-multimodal-intelligence-stabilization-v1.md`). Not a new intelligence capability. Root cause of the reported "Provider unavailable" image-analysis error: the live `ai-chat` Edge Function was running a stale build (v17) that predated all multimodal content-block support already present in the repository's own source — client-side provider resolution for image analysis was, and always had been, identical to normal chat's; the two paths only diverged once a request reached the deployed function, which had no translator for image content. Redeploying the current repo source (zero code changes needed) was the actual fix.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| P0 fix — Image analysis "Provider unavailable": the deployed `ai-chat` Edge Function (v17) predated the `ChatContentPart[]`/`toAnthropicContent`/`toOpenAiContent`/`toGoogleParts` multimodal support already written into the repo's own `supabase/functions/ai-chat/index.ts` — the stale version forwarded the client's internal `{type:'image', imageUrl}` shape straight into each provider's request body, never translating it into the real wire format (e.g. OpenAI's `{type:'image_url', image_url:{url}}`). Fixed by redeploying the current repo source verbatim (now v18) — no application code changed for this half of the fix | ⚙️ | main | ❌ | — | — (edge-function Deno redeploy; verified by the deployed source now matching the repo byte-for-byte, same as every other edge-function row in this table) |
| P0 hardening — explicit vision-capability declaration: no distinction between "chat-capable" and "vision-capable" existed anywhere in the provider registry or `resolveProviderChain`, even though the milestone's own audit checklist explicitly asked whether one did. New optional `AIProviderDescriptor.supportsVision` (omitted = vision-capable, matching every provider registered today) and `resolveProviderChain`'s new optional `requireVision` filter (a no-op unless a caller opts in) — `useAnalyzeImage` is the only caller that does. Automatic routing, fallback, platform governance, and Pro-preference-as-preference are completely unchanged for every existing call site | ⚙️ | main | ❌ | — | ✅ (`resolveProviderChain.test.ts` +5: excludes text-only providers, excludes even a preferred text-only provider, treats omitted `supportsVision` as capable, no-op when `requireVision` omitted, empty chain when nothing vision-capable is eligible) |
| Problem 3 fix — Notes "Analyze with NOVA": "Ask NOVA about this note" seeded a new conversation with only the note's title (`I'd like to talk about a note called "..."`) — the same bare-title seed-message bug the prior milestone already fixed for images, never fixed for notes. New `buildNoteAnalysisSeedQuery.ts` builds a real seed message from the note's actual content, requesting main ideas/decisions/dates/tasks/entities, with an honest empty-note fallback. Because a note's content can be arbitrarily long ("substantial work") and the existing `createConversationWithQuery` carries its seed text through a URL query param (unsafe at note length), a parallel `createConversationWithNoteAnalysis` action navigates with just the note id (`?initialNoteId=`) instead — `ChatPage.tsx` fetches the note and builds the seed message once mounted, mirroring the existing `initialQuery` effect exactly. Reuses the identical conversation-creation/send pipeline throughout — no second note-intelligence engine | ⚙️ | main | ❌ | — | ✅ (`buildNoteAnalysisSeedQuery.test.ts` 5: real content reaches the seed, all four extraction targets requested, honest empty-note fallback, whitespace-only treated as empty, content trimmed) |
| Explicitly deferred, with reasoning (see the v1 doc's "Deferred, not silently skipped" note): extending `retrieveContext`/`retrieveGraphContext` to also surface note content in *other*, unrelated ongoing chat conversations (the same symmetry the prior milestone's asset fix added) — not required to resolve the reported symptom; a calibrated live capability probe per provider rather than the current static `supportsVision` declaration — not needed while the declaration already matches every registered provider's real capability exactly; all 7 manual verification items in the v1 doc, none of which an automated test in this codebase can confirm | 🔲 | — | — | — | — |

## PIP Reliability Sprint 2/10 — Explicit Note Intelligence

Full discovery + implementation record in `docs/pip-reliability-sprint-2-note-intelligence.md`. Re-verified (not assumed) that the deployed `ai-chat` Edge Function still matches the repo after Milestone 1's redeploy — no drift found. Confirmed, by reading the migration directly rather than assuming, that `notes` RLS already correctly scopes access to the owner or a shared workspace member. The one real defect: `ChatPage.tsx`'s `initialNoteId` effect had no `.catch()` — a deleted or inaccessible note produced an unhandled promise rejection and a silently empty conversation, no feedback at all.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — graceful failure for a missing/inaccessible Note: extracted `resolveNoteAnalysisSeedText.ts` (fetch + build seed, with its own try/catch) out of `ChatPage.tsx`'s inline `getNote(...).then(...)`, which had no `.catch()` and silently swallowed a deleted-note or RLS-denied-note failure as an unhandled rejection. Now produces a visible, honest fallback message in the conversation instead, reusing `getNote`'s existing RLS-enforced behavior (throws identically for "doesn't exist" and "not yours," the correct non-information-leaking behavior for both) | ⚙️ | main | ❌ | — | ✅ (`resolveNoteAnalysisSeedText.test.ts` 3: real content reaches the seed, unauthorized/nonexistent note produces the graceful fallback without leaking the note id, deleted note produces the same fallback) |
| Hardening — extraction categories + URL-transport proof: `buildNoteAnalysisSeedQuery` now explicitly requests "priorities" (the acceptance contract's fifth category) and asks NOVA to keep categories distinct rather than blending them; `useCommandActions.ts`'s note-analysis URL building extracted into pure `buildNoteAnalysisConversationUrl.ts` so "only ever a note id, never content, regardless of note length" is directly tested rather than true only by inspection | ⚙️ | main | ❌ | — | ✅ (`buildNoteAnalysisSeedQuery.test.ts` +2, `buildNoteAnalysisConversationUrl.test.ts` 3) |
| Context integrity proof (Phase 6): new `AIService.test.ts` tests assert against the actual object passed to `streamChatCompletion` — the provider-call boundary — rather than an intermediate step: a realistic note-analysis seed (the sprint's own Phase 9 acceptance script content) appears verbatim in the provider request; a ~15,000-character note is not truncated; prior conversation `history` plus a follow-up question produces the exact expected `messages` array (proving follow-up context retention); a note-analysis message goes through the identical provider/fallback shape as any other message (no note-specific provider path) | ⚙️ | main | ❌ | — | ✅ (`AIService.test.ts` +4: seed content reaches the provider verbatim, no truncation on a long note, follow-up retains history, identical fallback shape) |
| Explicitly deferred, with reasoning (see the sprint doc's "Deliberately deferred" section): extending `retrieveContext`/`retrieveGraphContext` to surface note content in *other*, unrelated ongoing conversations — the explicit seed-message approach is what the acceptance contract actually requires, not RAG symmetry; a dedicated `ChatPage.tsx` component-level test — no React Testing Library component test exists anywhere in this codebase for a page this size, so the properties that matter are proven at the smaller unit level instead; Phase 9 live acceptance itself — this session has no authenticated browser session against the deployed app, so the exact acceptance script (budget/photography/payment-gateway/reasoning/follow-up questions) requires a human to run and confirm | 🔲 | — | — | — | — |

## PIP Sprint 3/10 — Spreadsheet Intelligence Validation

Full discovery + implementation record in `docs/spreadsheet-intelligence-v1-discovery.md` and `docs/spreadsheet-intelligence-v1.md`. Confirmed the existing deterministic analysis engine (`src/modules/processing/spreadsheet/`) was already structured (not flattened prose), already wired into chat/Universal Search/the Knowledge Graph via the generic document pipeline, and already provider-agnostic — no second spreadsheet engine, no new provider path. Two narrow, confirmed defects were fixed in that same engine: bare month-label period columns ("Jan"/"Feb", no year — the common real-world case) were silently excluded from every grouping, and category breakdowns were only ever computed for one "primary" numeric column, so a second currency column (e.g. Cost, alongside Revenue) never got its own breakdown — both blocked real acceptance questions (highest-revenue month, gross-profit-by-product).

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — month-label period columns now group correctly: `analyzeSheet`'s category-breakdown eligibility now also includes `meaning: 'timeline'` columns whose `dataType` isn't a genuine parsed `'date'` (verified with Node directly: `parseDateValue("Jan")` is `Invalid Date` — no year present) — exactly the case where a "Month" column with values like Jan/Feb/Mar was detected as period-like but excluded from every grouping. Genuine full-date columns are untouched, still covered by the existing month-grouped `trends`, so nothing is computed twice | ⚙️ | main | ❌ | — | ✅ (`workbookAnalysis.test.ts` +3) |
| Fix — every numeric column gets its own category breakdown, not just one: `categoryBreakdowns` is now the full (category column × numeric column) product (capped at `MAX_BREAKDOWNS_PER_SHEET = 20`/sheet, mirroring the existing per-breakdown row/anomaly caps), so Cost-by-Product now exists alongside Revenue-by-Product — required for "which product had the highest gross profit" | ⚙️ | main | ❌ | — | ✅ (`workbookAnalysis.test.ts` +2, `workbookAnalysis.acceptance.test.ts` new: 7 tests against the Phase 8 fixture covering Tests B/C/D/E/F/G/H) |
| Coverage added for previously-untested layers: real `XLSX.write`/`XLSX.read` round-trip extraction (multi-sheet, formula-column detection off the raw worksheet, a real CSV file through the same code path) and `retrieveSpreadsheetContext`'s never-throws/no-documentId/non-spreadsheet-document contract, neither of which had any test before this sprint | ⚙️ | main | ❌ | — | ✅ (`extractors/spreadsheet.test.ts` new: 5, `api/retrieveSpreadsheetContext.test.ts` new: 4) |
| Context integrity proof: new `AIService.test.ts` tests assert the `<spreadsheet_analysis>` block reaches the actual provider request (not an intermediate function), is absent with no `documentId`, is recomputed — not merely replayed via history — on a follow-up turn (the mechanism Test J's "why was February stronger than March" follow-up depends on), and routes through the identical provider/fallback shape as any other message | ⚙️ | main | ❌ | — | ✅ (`AIService.test.ts` +4, "Spreadsheet analysis context integrity") |
| Data-quality finding, documented rather than silently absorbed: the Phase 8/9 acceptance fixture's own numbers produce a genuine tie (February and March both total 20,000 revenue) and make Test J's premise ("why was February stronger") false on both counts that were checked — March actually has higher gross profit and higher combined (revenue − cost − opex) performance than February. Not a code defect; recorded in the v1 doc as ground truth so it isn't mistaken for one | 🔲 | — | — | — | ✅ (`workbookAnalysis.acceptance.test.ts` pins the exact tie and the March-is-stronger result) |
| Explicitly deferred, with reasoning (see the discovery doc): formula text + cell coordinate + value as a structured (Sheet, Cell, Formula, Value) triple — confirmed absent (only a per-column `hasFormulas` boolean exists), but no acceptance test requires it and it's a genuinely separate feature; `.xls` (legacy binary) ingestion — not registered, not required by any acceptance test, the task itself treats it as optional; renaming the generic "Chat with NOVA" entry point to a spreadsheet-specific "Analyze with NOVA" — a platform-wide wording change touching every document type, outside this sprint's scope; Phase 9 live acceptance itself — this session has no authenticated browser session against the deployed app, so whether a live model actually declines Test J's false premise (rather than merely having correct grounding data available) requires a human to run and confirm | 🔲 | — | — | — | — |

## PIP Sprint 4/10 — Document Intelligence Validation & Hardening

Full discovery + implementation record in `docs/document-intelligence-v1-discovery.md` and `docs/document-intelligence-v1.md`. Diagnosed the real reported failure ("What has been mentioned about ARRIYIA in the article?") down to two confirmed code defects, not a wording problem: (1) `retrieveContext` (chat's only retrieval call) was pure vector cosine similarity with zero lexical/exact-match fallback anywhere in the chat path — a rare proper noun mentioned once in a long document could be entirely absent from the top-8 semantic matches; (2) even a correctly-retrieved chunk was never labeled with its source document/page in the model's own context — `referenceResolver.ts` already fetched that exact data every turn, but only *after* the LLM answered, for the UI's reference chips. Both fixed inside the existing retrieval/context-assembly pipeline — no second search engine, no new tables, no edge function changes.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — hybrid semantic + lexical retrieval: `extractLexicalSearchTerms.ts` (new, pure) extracts entity-like terms (ALL-CAPS, quoted phrases, mid-sentence Title-Case) from a chat question; `lexicalChunkSearch.ts` (new) runs a literal `ILIKE` search over `document_chunks.content` via the authenticated Supabase client, relying on the table's existing RLS policy (verified identical to `match_document_chunks`'s own security predicate — no new SQL function needed); `retrieveContext` merges semantic + lexical results using the exact `applyLexicalBoost`/`LEXICAL_ONLY_BASE_SCORE` constants Universal Search's `documentSearchProvider.ts` already established, not a new scoring system | ⚙️ | main | ❌ | — | ✅ (`extractLexicalSearchTerms.test.ts` new: 9, `retrieveContext.test.ts` new: 6) |
| Fix — provenance now reaches the model, not just the UI: `resolveChunkProvenance.ts` (new) reuses the exact `getChunkLocations`/`getDocumentTitles` calls `referenceResolver.ts` already made, resolved before `buildSystemPrompt` instead of after; `buildSystemPrompt` gained one new optional, additive parameter (`chunkProvenance?: Map<chunkId,string>`) — when present, labels each context excerpt `(Document Title — Page N)`, never fabricating a page when none exists; omitted (every pre-Sprint-4 call site), output is byte-identical to before | ⚙️ | main | ❌ | — | ✅ (`resolveChunkProvenance.test.ts` new: 5, `buildSystemPrompt.test.ts` +3) |
| Hardening — prompt-injection defense: the `rag-chat@1.0` template already told the model to answer only from context and admit missing evidence; added one explicit sentence that excerpts are evidence to reason about, never instructions to follow, even if an excerpt's text looks like a command | ⚙️ | main | ❌ | — | — (prompt text only; covered indirectly by every existing `buildSystemPrompt`/`AIService` test still passing unchanged) |
| Phase 3 deterministic reproduction: `__fixtures__/arriyiaArticle.ts` — a realistic 8-page article, chunked with the real `chapterAwareChunker` (not synthetic chunks), with ARRIYIA appearing exactly once on page 6 — proves the fix against a reconstruction of the actual reported failure, not just isolated unit tests | ⚙️ | main | ❌ | — | ✅ (`documentIntelligence.arriyia.test.ts` new: 5 — retrieval despite a semantic miss, real page-6 provenance, honest empty-context for a nonexistent entity, position-independence at start/end) |
| Explicitly deferred, with reasoning (see the discovery doc): reranking/query expansion — the confirmed defect was absence from the result set, not poor ordering, so fixing presence was the priority; OCR/scanned-PDF extraction — confirmed absent, same finding already recorded in `multimodal-intelligence-v2-discovery.md`, a genuinely separate larger feature; Phase 10 live acceptance itself — this session has no authenticated browser session against the deployed app, so whether a live model's phrasing satisfies the qualitative acceptance tests (concise explanation, sentiment reasoning, insight quality) requires a human to run and confirm | 🔲 | — | — | — | — |

## PIP Sprint 5/10 — Knowledge Graph & Intelligence Validation

Full discovery + implementation record in `docs/knowledge-intelligence-layer-v2-discovery.md` and `docs/knowledge-intelligence-layer-v2.md`. Validation sprint: confirmed nearly all graph/confidence/timeline/gap infrastructure (Knowledge Intelligence Layer v1) was already correct — entity resolution already unifies case/whitespace/punctuation variants without fuzzy over-merging, cross-source evidence accumulation already treats document/note/conversation/asset sources as equally corroborating, timeline and gap detection already work generically. One real, structural gap found and fixed: `retrieveGraphContext` (chat's `<knowledge_connections>` source) only ever surfaced nodes sourced from documents/assets a *chunk* search already matched — a chat question naming an entity directly had no path to that entity's real graph evidence (`getKnowledgeNodeEvidence`, already built and correct, but unreachable from chat). No parallel graph, recommendation, or intelligence engine was built.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — named entities reach chat's graph context directly: `retrieveNamedEntityGraphContext.ts` (new) reuses `extractLexicalSearchTerms` (Sprint 4) to pull entity terms from the question, `normalizeTitle` (Sprint 9A) for exact-normalized node lookup, and `getKnowledgeNodeEvidence` (KIL v1) — the same rich evidence/confidence/relationship data the Knowledge Explorer UI already uses — for whatever matches. Merged alongside (not replacing) the existing chunk-sourced `retrieveGraphContext` result in `AIService.ts`; both share the same `"Concept:"/"Entity:"` line convention, so `buildSystemPrompt`'s signature and `buildContextTrace`'s counting needed no changes | ⚙️ | main | ❌ | — | ✅ (`retrieveNamedEntityGraphContext.test.ts` new: 9, `AIService.test.ts` +4, "Named-entity graph context") |
| Hardening — confidence framed in plain language, not bare numbers: a node below 0.6 confidence gets an inline "(limited evidence — treat as provisional)" or "(very limited evidence — treat with caution)" caveat directly in the context text; a relationship with zero shared corroborating sources is labeled "inferred, not directly corroborated" rather than presented flatly alongside a well-evidenced one — reuses `RelationshipConfidence.evidenceCount`/`.sources` (KIL v1), no new scoring formula | ⚙️ | main | ❌ | — | ✅ (covered within `retrieveNamedEntityGraphContext.test.ts`'s formatting tests) |
| Discovery confirmation, not a code change: existing entity resolution (`normalizeTitle` + `decideResolutionAction`'s `reuse` case) already correctly unifies `ARRIYIA`/`Arriyia`/`arriyia` and correctly accumulates evidence across a document, note, spreadsheet, and image mentioning the same entity, without fuzzy over-merging — verified by reading and re-confirming existing, already-passing tests rather than duplicating them | 🔲 | — | — | — | ✅ (pre-existing `normalizeTitle.test.ts`, `knowledgeNodeResolution.test.ts` — unchanged, re-verified) |
| Explicitly deferred, with reasoning (see the discovery doc): conflict/contradiction detection — confirmed genuinely absent, and the existing confidence-model code already documents this as needing new pairwise-LLM infrastructure it doesn't have; not attempted, to avoid building a rushed, untested new engine within this sprint; fuzzy/abbreviation entity matching — deliberately not built, consistent with "do not aggressively merge entities without sufficient evidence"; Graph Workspace's document-only relationship-strength ranking — a real, minor, pre-existing inconsistency with the cross-source-aware evidence function chat now uses, left alone since fixing it means touching graph visualization with no genuine blocking defect found there; automatic full extraction on note/conversation save — confirmed intentional (cost-conscious), not changed; Phase 18 live acceptance itself — this session has no authenticated browser session against the deployed app | 🔲 | — | — | — | — |

## PIP Sprint 6/10 — Memory & Personalization Intelligence Validation

Full discovery + implementation record in `docs/memory-personalization-v1-discovery.md` and `docs/memory-personalization-v1.md`. Validation sprint: confirmed nearly all memory infrastructure (storage, RLS, human-approved detection, structured profile fields, confidence scoring, deletion, management UI) was already correct. One real, structural gap found and fixed: `retrieveMemoryContext` (chat's `<personal_context>` source) never received the current question's text at all — it injected up to 30 stored memories (10 per type) on every single turn regardless of topic, with no way to exclude an irrelevant one. No second memory/personalization engine was built.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — memory retrieval becomes relevance-aware: `filterMemoriesByRelevance.ts` (new) reuses `extractLexicalSearchTerms` (Sprint 4/5) to exclude `conversation_memory` entries unrelated to the current question, while `explicit_profile`/`learned_preference` — durable identity/style personalization, not topic-scoped — always pass through unfiltered. `retrieveMemoryContext.ts` gained a required `text` parameter (`AIService.ts` now passes it through, matching `retrieveContext`/`retrieveNamedEntityGraphContext`'s call shape) | ⚙️ | main | ❌ | — | ✅ (`filterMemoriesByRelevance.test.ts` new: 9, `retrieveMemoryContext.test.ts` new: 7, `AIService.test.ts` updated: 1) |
| Hardening — `MEMORY_SAFETY_NOTE` extended with two clauses reusing the existing note: an evidence-not-instruction guard for `<personal_context>` (mirroring the guard `{{context}}` has had since Sprint 4), and explicit "trust the more-recently-listed entry" framing for same-topic conflicts — turns `rankMemories`' existing most-recently-updated-first ordering into a real, honest (if partial) answer to preference supersession, without a new dedup/contradiction system | ⚙️ | main | ❌ | — | ✅ (`buildSystemPrompt.test.ts` +2) |
| Trust-accuracy fix — `MemoryCard.tsx`'s "Used by NOVA" tooltip copy updated so it no longer implies unconditional inclusion for `conversation_memory` now that eligibility is relevance-gated; `isMemoryUsedByPrompt`'s underlying rank/cap computation is unchanged, only the copy | ⚙️ | main | ❌ | — | — (copy-only; existing `memoryPromptUsage.test.ts` still passes unchanged) |
| Discovery confirmation, not a code change: current-instruction precedence (Test E), user isolation via RLS, and the workspace/source boundary (no document/note/workspace content ever writes to `ai_memory`) were all already correct — verified by reading the actual RLS policy SQL and every `createMemory` call site, not assumed | 🔲 | — | — | — | ✅ (pre-existing `formatMemoriesForPrompt.test.ts`'s ordering tests, `detectMemoryCandidates.test.ts` — unchanged, re-verified) |
| Explicitly deferred, with reasoning (see the discovery doc): fuzzy/semantic memory deduplication — would require a new embeddings/LLM comparison capability, a genuinely separate feature, not attempted; workspace-scoped vs. global memory pools not merging across a workspace switch — a real, pre-existing UX-13.6/UX-5.x behavior, ambiguous which is "more correct" without a product decision, not touched; Phase 10 live acceptance itself — this session has no authenticated browser session against the deployed app | 🔲 | — | — | — | — |

## PIP Sprint 7/10 — Search & Retrieval Unification

Full discovery + implementation record in `docs/search-retrieval-unification-v1-discovery.md` and `docs/search-retrieval-unification-v1.md`. Validation and consolidation sprint: confirmed ARRIYIA has two parallel retrieval paths (chat grounding vs. Universal Search's `SearchProvider` registry) that legitimately share the same pgvector RPCs and `hybridScore.ts` lexical logic at two different granularities, not one coherent system and not wasteful duplication either. One real, structural gap found and fixed: notes have had full hybrid semantic+lexical search in Universal Search for two sprints, but chat's own retrieval path never queried them at all — a note-only fact was unreachable from chat unless a knowledge-graph node for it already existed from a different source. A second, smaller pre-existing security gap (found during the same audit) was closed alongside it. No second search/ranking/knowledge engine was built.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — notes reach chat directly: `retrieveNoteContext.ts` (new) reuses `match_notes` (the same RPC Universal Search's `notesSearchProvider` already calls) for semantic search, plus a new content-level lexical fallback mirroring `lexicalChunkSearch.ts`'s exact shape (chat needs to find the passage mentioning a rare term, not just a note whose title matches). `buildSystemPrompt.ts` gained a `noteMatches` parameter (appended last, no existing call site changed) rendering a `<note_context>` block labeled `(Note: Title)` per match; `AIService.ts` wires it in with the same never-throws-at-the-call-site contract every other optional context source already follows | ⚙️ | main | ❌ | — | ✅ (`retrieveNoteContext.test.ts` new: 7, `buildSystemPrompt.test.ts` +6, `AIService.test.ts` +11) |
| Hardening — evidence-not-instruction guard extended to `<visual_context>`: both notes and assets are workspace-shareable (confirmed by reading their actual RLS policies), so their content can legitimately originate from a different workspace member, same as documents. The guard `{{context}}` has had since Sprint 4 is now shared verbatim by `<visual_context>` and the new `<note_context>` block — a real, pre-existing gap on the asset side, found during this sprint's own security audit rather than left for a future one | ⚙️ | main | ❌ | — | ✅ (covered within `buildSystemPrompt.test.ts`'s injection-guard tests) |
| Discovery confirmation, not a code change: document chunk retrieval, asset semantic retrieval, spreadsheet retrieval, knowledge-graph/named-entity retrieval, memory retrieval, chunk-level deduplication, empty-result honesty, and the hybrid+lexical ranking model were all already correct — no new ranking weights were invented anywhere, per the task's own "do not arbitrarily invent new weights" instruction; no observed failure justified one | 🔲 | — | — | — | ✅ (8 explicit Phase 8 cross-feature tests in `AIService.test.ts` proving multiple sources combine correctly in one prompt — Tests A/B, C, D, E, G, H, I, J) |
| Explicitly deferred, with reasoning (see the discovery doc): cross-conversation retrieval (past chats as chat grounding evidence) — a real gap, but a materially larger, more product-sensitive feature than wiring up an existing source type, with no required test needing it; `retrieveAssetContext`'s missing lexical fallback — lower severity than the notes gap since images already reach chat with real content via semantic search; UI reference chips for notes/assets/graph/memory — a citation-UI feature, not a model-evidence-access gap; Phase 9 live acceptance itself — this session has no authenticated browser session against the deployed app | 🔲 | — | — | — | — |

## PIP Sprint 8/10 — Reliability & Error-Handling Validation

Full discovery + implementation record in `docs/pip-reliability-v1-discovery.md` and `docs/pip-reliability-v1.md`. Hardening sprint: confirmed the provider-routing, document-processing, and retrieval reliability architecture built across Sprints 4-7 is already mature — a real, reused error taxonomy (`normalizeAiError.ts`), honest processing-job failure states, rate-limit retry with exponential backoff, and a consistent "optional context source never breaks the whole turn" contract. Three real, confirmed defects were found and fixed; no second error-handling framework, retry system, or provider-routing layer was built.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — six silent, unlogged retrieval-failure sites now traceable: `retrieveGraphContext`, `retrieveMemoryContext`, `retrieveSpreadsheetContext`, `retrieveNamedEntityGraphContext` (internal `catch { return null }`) and `retrieveAssetContext`/`retrieveNoteContext` (external `.catch()` in `AIService.ts`) all correctly never broke a chat turn on failure — but logged nothing, so a genuine ongoing failure was indistinguishable from "this source found nothing," even server-side. Added one `console.error` per site, reusing the exact logging convention already established by `processDocument.ts`/`indexNote.ts` — no new logging framework, no change to the never-throws contract, nothing exposed to the user | ⚙️ | main | ❌ | — | ✅ (covered by each function's existing never-throws-contract tests; behavior unchanged, side-effect-only addition) |
| Fix — `useAnalyzeImage.ts` no longer discards a successful vision analysis when knowledge extraction or document intelligence subsequently fails: the core analysis is now persisted the moment it succeeds, and the two enrichment steps are best-effort (logged, not fatal) rather than all-or-nothing. Previously a downstream, non-essential failure made a successfully analyzed image look exactly as if it had never been analyzed, forcing a full costly retry | ⚙️ | main | ❌ | — | ✅ (`useAnalyzeImage.test.ts` new: 4 — no test existed for this hook before this sprint) |
| Fix — `quotaService.checkQuota` no longer reports a genuine database failure identically to "no active plan": still fails closed (never risks unmetered usage), but now returns a distinct, honest reason ("Could not verify your plan — please try again.") when the assignment/quota query itself errored, with the real error logged for diagnosis | ⚙️ | main | ❌ | — | ✅ (`quotaService.test.ts` +1, 1 existing test updated to the new honest reason) |
| Discovery confirmation, not a code change: provider fallback/categorized-errors/streaming safety, vision-provider filtering, document processing failure handling, spreadsheet empty/malformed handling, and knowledge-graph/memory never-fabricates-evidence were all already correct — re-verified by reading and re-testing the actual code, not assumed | 🔲 | — | — | — | ✅ (re-confirmed via existing, unchanged test suites for each area) |
| Explicitly deferred, with reasoning (see the discovery doc): no structured "which source failed" signal reaches the model or UI — extending six functions' return contracts across four prior sprints is a larger, separate change; UI-level error-state redesign — every surface already shows a real, specific error, no defect found requiring one; Phase 12 live acceptance itself — this session has no authenticated browser session against the deployed app | 🔲 | — | — | — | — |

## PIP Sprint 9/10 — Performance & Scale Validation

Full discovery + implementation record in `docs/pip-performance-v1-discovery.md` and `docs/pip-performance-v1.md`. Validation/hardening sprint: confirmed per-source retrieval caps, Universal Search's already-concurrent providers, rate-limit-safe ingestion batching, and near-complete database index coverage were all already correct. Seven real, evidence-based defects were found and fixed — no second retrieval engine, caching system, indexing system, or other new infrastructure was introduced.

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Fix — the chat query was embedded three times per turn (`retrieveContext`, `retrieveAssetContext`, `retrieveNoteContext` each independently called the embedding provider on identical text). `AIService.ts` now embeds once and shares the result via a new optional `embedding` param on each function; each still embeds internally when the param is omitted, so standalone/test usage is unchanged | ⚙️ | main | ❌ | — | ✅ (`retrieveContext.test.ts`/`retrieveNoteContext.test.ts` +2 each, `retrieveAssetContext.test.ts` new, `AIService.test.ts` +4) |
| Fix — six independent retrieval sources (`retrieveContext`, `retrieveAssetContext`, `retrieveNoteContext`, `retrieveNamedEntityGraphContext`, `retrieveMemoryContext`, `retrieveSpreadsheetContext`) ran in strict sequence despite none consuming another's result; now run concurrently via `Promise.all` in `AIService.ts`. The two sources that genuinely depend on that batch's output (`retrieveGraphContext`, `resolveChunkProvenance`) form a second, still-concurrent-with-each-other stage. Every existing never-throws contract (Sprint 7/10, Sprint 8/10) unchanged | ⚙️ | main | ❌ | — | ✅ (covered by `AIService.test.ts`'s full existing suite, unchanged behavior, plus 4 new Sprint 9/10 tests) |
| Fix — `retrieveContext`/`retrieveNoteContext` each ran their own semantic RPC and lexical `ILIKE` search sequentially internally, despite the two not depending on each other; now run concurrently via `Promise.all` | ⚙️ | main | ❌ | — | ✅ (covered by each file's existing hybrid-search test suite, unchanged behavior) |
| Fix — `quotaService.checkQuota` ran after the entire retrieval/prompt-construction pass instead of before it, so a user already over quota still paid for a full retrieval pass before being rejected. Moved to immediately after the user message is inserted, before any retrieval or embedding work | ⚙️ | main | ❌ | — | ✅ (`AIService.test.ts`: a rejected turn never calls a single retrieval source or the embedding provider) |
| Fix — conversation history sent to the model had no bound: `ChatPage.tsx`/`ReaderChatPanel.tsx` sent a conversation's *entire* message list to the provider on every turn, growing prompt size/latency/cost without limit as a conversation aged. New `buildChatHistory` helper (shared by both call sites) keeps only the most recent 40 messages; the underlying unbounded `listMessages`/`useMessages` fetch is untouched, since the UI must still display full history | ⚙️ | main | ❌ | — | ✅ (`buildChatHistory.test.ts` new, 5 tests) |
| Fix — `retrieveMemoryContext`'s `listMemories` call fetched the user's entire active memory table, unbounded, on every chat turn, discarding all but ~30 rows after relevance filtering/ranking. Added `.limit(200)` — ordered by `updated_at desc`, matching `rankMemories`'s own tie-break, so the cap doesn't change which memories are selected for any realistic account | ⚙️ | main | ❌ | — | ✅ (`retrieveMemoryContext.test.ts` updated to assert the new limit) |
| Fix — `ai_memory` had zero indexes beyond its primary key since the table's creation; every query has been a full table scan. Additive migration `0040_performance_indexes.sql` adds `ai_memory (user_id, is_active, updated_at desc)` and `messages (conversation_id, created_at)` (the existing single-column index served the filter but not the sort) | ⚙️ | main | ❌ | ✅ (query-plan verification against real data) | — |
| Discovery confirmation, not a code change: per-source retrieval result caps, Universal Search's concurrent provider execution and batched evidence-count query, the ingestion pipeline's rate-limit-safe sequential embedding batching, and pgvector/table index coverage everywhere else were all already correct — re-verified by reading and re-testing the actual code, not assumed | 🔲 | — | — | — | ✅ (re-confirmed via existing, unchanged test suites for each area) |
| Explicitly deferred, with reasoning (see the discovery doc): no route-level code splitting (single ~1.27MB main bundle, no measured evidence of harm); `listDocuments`/Library page fetch unbounded with no pagination UI to consume a query-level limit safely; no trigram index for document/note content lexical search (already scoped by indexed columns first, no evidence of current cost); live load-testing against a genuinely large knowledge base — this environment can't generate or measure one | 🔲 | — | — | — | — |

## PIP Sprint 9.5/10 — Backlog, Technical Debt & Release Scope Review

Not a feature sprint — a full-repository audit reconciling every deferred item across Sprints 1-9 against current reality, producing the authoritative pre-freeze release definition. Full record in `docs/pip-release-scope-v1.md` (what v1 is and isn't), `docs/arriyia-personal-release-backlog.md` (the itemized P0/P1/P2/Strategic/Obsolete backlog), and `docs/arriyia-product-roadmap.md` (Personal → Business → Enterprise strategic direction, not implemented).

| Item | Status | Branch | Accepted | Manual | Tests |
|---|---|---|---|---|---|
| Audit confirmation: no P0 (release-blocking) issue found — nine prior sprints already closed every defect at that severity found along the way; this audit's own fresh pass (backlog-marker search, every prior "known limitations" section reconciled, end-to-end journey retrace) found nothing new at that severity | 🔲 | — | — | — | — |
| Finding — full account deletion does not exist (individual content is deletable; a single "delete everything" action is not). Classified P1, deliberately **not** autonomously built this sprint per Phase 7's own criteria: two reasonable designs exist (hard vs. soft delete), it touches every user-owned table plus Storage, and getting it wrong is worse than deferring it. Recorded as the top pre-general-availability priority | 🔲 | — | — | — | — |
| Finding — two dead-code items found and classified Obsolete: `semanticChunker` (registered, never selected by the ingestion pipeline, throws if ever invoked) and `PlaceholderEmbeddingProvider` (implements the interface, zero production call sites). Neither poses current risk; both recommended for removal in a future cleanup, not this sprint | 🔲 | — | — | — | — |
| Finding — asset-import provenance documentation was stale: `assetPackageTypes.ts` claims assets have no JSONB metadata column, but `assets.metadata` has existed since the Multimodal Intelligence v1 migration. The underlying capability (an `importedFrom` block, present for Notes/Knowledge Nodes) was still never built for Assets — corrected in the backlog as a documentation fix plus a genuine, low-severity P2 gap | 🔲 | — | — | — | — |
| Product-boundary audit: workspace collaboration/sharing is real, shipped, tested functionality that structurally belongs to ARRIYIA Business more than Personal — explicitly not removed (it works, it's tested), but flagged in `docs/arriyia-product-roadmap.md` as the one place Personal already carries organizational-shaped scope | 🔲 | — | — | — | — |
| Release scope decision: nothing must happen before Sprint 10. Every discovered item has an explicit destination (P2/Business/Enterprise/Future/Obsolete) in the new backlog documents; none require resolution to proceed | 🔲 | — | — | — | — |
