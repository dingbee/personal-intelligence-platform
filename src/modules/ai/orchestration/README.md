# AI Orchestration

Coordinates prompt construction, provider selection, retrieval calls, and
memory for a chat turn. Sits between the chat UI (`modules/ai/chat`) and the
provider adapters (`modules/ai/providers`).

Not implemented yet — this module is scaffolded for a later milestone
(AI chat with RAG).
