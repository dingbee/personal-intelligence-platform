# Embeddings

Generates and stores vector embeddings for document chunks and notes using
pgvector. Consumed by `retrieval/` for similarity search.

Not implemented yet — this module is scaffolded for a later milestone
(document processing and indexing).
