# Retrieval

Runs similarity search over embeddings and assembles grounding context for
RAG chat and semantic search. Consumed by `orchestration/` and the search
module.

Not implemented yet — this module is scaffolded for a later milestone
(semantic search).
