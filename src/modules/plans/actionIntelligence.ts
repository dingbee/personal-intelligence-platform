/**
 * ARRIYIA Professional Intelligence — Action Intelligence.
 *
 * A dedicated feature key, following the exact precedent set by every
 * sibling professional-intelligence engine (Data/Analysis/Research/
 * Planning/Decision). Seeded for pro/founding_pro only in
 * 0064_action_intelligence_entitlement.sql, resolved through the same
 * plan_quotas/has_feature machinery as every other feature flag — no new
 * entitlement mechanism.
 */
export const ACTION_INTELLIGENCE_FEATURE_KEY = 'action_intelligence'
