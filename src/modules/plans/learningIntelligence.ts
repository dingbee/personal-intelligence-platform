/**
 * ARRIYIA Professional Intelligence — Learning / Adaptive Intelligence (I8).
 *
 * A dedicated feature key, following the exact precedent set by every
 * sibling professional-intelligence engine. Seeded for pro/founding_pro
 * only in 0087_learning_intelligence_foundation.sql, resolved through the
 * same plan_quotas/has_feature machinery as every other feature flag — no
 * new entitlement mechanism.
 */
export const LEARNING_INTELLIGENCE_FEATURE_KEY = 'learning_intelligence'
